---
# GENERATED by scripts/sync-docs.mjs from gmedia/usai@v0.0.11:docs/sdk/test/interfaces/TestResponse.md — do not edit; fix upstream and re-sync.
title: "Interface: TestResponse"
slug: "sdk/test/interfaces/testresponse"
source: "docs/sdk/test/interfaces/TestResponse.md"
---
[@sakaladev/usai](/docs/sdk/) / [test](/docs/sdk/test/) / TestResponse



One HTTP response from the application under test: `body` is the
parsed JSON when the response was JSON, otherwise the text.

## Properties

| Property | Type | Description |
| ------ | ------ | ------ |
| <a id="status"></a> `status` | `number` | - |
| <a id="headers"></a> `headers` | `Record`\<`string`, `string`\> | - |
| <a id="body"></a> `body` | `unknown` | - |
| <a id="text"></a> `text` | `string` | - |
| <a id="bytes"></a> `bytes` | `Uint8Array` | The response body's exact bytes (a downloaded file, a binary raw response). |
| <a id="violations"></a> `violations` | `string`[] | Lifecycle violations the request's world committed (`detached_work`, …), from the `x-usai-lifecycle` header the harness's runtime exposes. A request that followed the rules has `[]`; assert on it. **Codes only**, because a response header is all a client gets. The other `violations` — [WorkOutcome.violations](/docs/sdk/test/interfaces/workoutcome/#violations), from `invoke` and `run` — is `{ code, message }[]`: there the harness holds the world's own result, and a violation's message *is* its explanation. |

