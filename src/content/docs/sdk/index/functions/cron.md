---
# GENERATED by scripts/sync-docs.mjs from gmedia/usai@v0.0.9:docs/sdk/index/functions/cron.md — do not edit; fix upstream and re-sync.
title: "Function: cron()"
slug: "sdk/index/functions/cron"
source: "docs/sdk/index/functions/cron.md"
---
[@sakaladev/usai](/docs/sdk/) / [index](/docs/sdk/index/) / cron



```ts
function cron<R extends ResourceDeclaration<string, unknown>[] = ResourceDeclaration<string, unknown>[]>(
   name: string, 
   options: CronOptions<R>, 
   handler: (ctx: CronContext<R>) => unknown
): Workload;
```

Declare a scheduled job. Each due tick runs in a **fresh world**, finite,
bounded by `timeout`. The scheduler belongs to the revision: it starts
when the revision activates and stops when it drains, so two revisions
never tick the same job at once. A missed tick (the process was down)
is not replayed. `usai cron run <name>` runs one tick without the
clock, and `app.cron(name).run()` does the same in tests.

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `R` *extends* [`ResourceDeclaration`](/docs/sdk/index/interfaces/resourcedeclaration/)\<`string`, `unknown`\>[] | [`ResourceDeclaration`](/docs/sdk/index/interfaces/resourcedeclaration/)\<`string`, `unknown`\>[] |

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `name` | `string` | Unique within the application; the id is `cron:<name>`. |
| `options` | [`CronOptions`](/docs/sdk/index/interfaces/cronoptions/)\<`R`\> | `schedule` (required), `overlap`, resources, `timeout`. |
| `handler` | (`ctx`: [`CronContext`](/docs/sdk/index/interfaces/croncontext/)\<`R`\>) => `unknown` | Runs once per tick. |

## Returns

[`Workload`](/docs/sdk/index/interfaces/workload/)

## Example

```ts
export const markOverdue = publishes(
  cron("mark-overdue", { schedule: "15 0 * * *", resources: [db] }, async (ctx) => {
    const rows = await ctx.resources.db.query("update invoices … returning id");
    for (const row of rows) await ctx.queue.publish("webhook.deliver", { event: "invoice.overdue", id: row.id });
  }),
  "webhook.deliver",
);
```

