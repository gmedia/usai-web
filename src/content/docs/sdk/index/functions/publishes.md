---
# GENERATED by scripts/sync-docs.mjs from gmedia/usai@v0.0.11:docs/sdk/index/functions/publishes.md — do not edit; fix upstream and re-sync.
title: "Function: publishes()"
slug: "sdk/index/functions/publishes"
source: "docs/sdk/index/functions/publishes.md"
---
[@sakaladev/usai](/docs/sdk/) / [index](/docs/sdk/index/) / publishes



```ts
function publishes<W extends Workload>(from: W, ...topics: (string | Workload)[]): W;
```

Record that `from` publishes to queue topics (names, or the consuming
`queue.consume` workloads) with [QueueHandle.publish](/docs/sdk/index/interfaces/queuehandle/#publish), for `usai
graph` and the reference page (the consumer's page lists its
publishers). Annotation only; the publish itself is `ctx.queue.publish`.
Returns `from` with its own type, so it wraps a declaration in place
without erasing what a task's handler returns.

## Type Parameters

| Type Parameter |
| ------ |
| `W` *extends* [`Workload`](/docs/sdk/index/interfaces/workload/) |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `from` | `W` |
| ...`topics` | (`string` \| [`Workload`](/docs/sdk/index/interfaces/workload/))[] |

## Returns

`W`

