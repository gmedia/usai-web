---
# GENERATED by scripts/sync-docs.mjs from gmedia/usai@v0.0.11:docs/sdk/index/functions/command.md — do not edit; fix upstream and re-sync.
title: "Function: command()"
slug: "sdk/index/functions/command"
source: "docs/sdk/index/functions/command.md"
---
[@sakaladev/usai](/docs/sdk/) / [index](/docs/sdk/index/) / command



## Call Signature

```ts
function command(name: string, handler: (ctx: CommandContext) => unknown): Workload;
```

Declare a command: finite work run on demand from the command line
(`usai app <name> [args]`), in a fresh world with the declared resources.
The return value is printed as JSON; a thrown error exits non-zero.
Commands are for operators (a stats report, a one-off repair), not for
startup: nothing runs a command unless someone asks.

### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |
| `handler` | (`ctx`: [`CommandContext`](/docs/sdk/index/interfaces/commandcontext/)) => `unknown` |

### Returns

[`Workload`](/docs/sdk/index/interfaces/workload/)

### Example

```ts
export const stats = command("invoices:stats", { resources: [db] }, async (ctx) =>
  ctx.resources.db.one("select count(*)::int as invoices from invoices"),
);
```

## Call Signature

```ts
function command<R extends ResourceDeclaration<string, unknown>[] = ResourceDeclaration<string, unknown>[]>(
   name: string, 
   options: CommandOptions<R>, 
   handler: (ctx: CommandContext<R>) => unknown
): Workload;
```

Declare a command: finite work run on demand from the command line
(`usai app <name> [args]`), in a fresh world with the declared resources.
The return value is printed as JSON; a thrown error exits non-zero.
Commands are for operators (a stats report, a one-off repair), not for
startup: nothing runs a command unless someone asks.

### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `R` *extends* [`ResourceDeclaration`](/docs/sdk/index/interfaces/resourcedeclaration/)\<`string`, `unknown`\>[] | [`ResourceDeclaration`](/docs/sdk/index/interfaces/resourcedeclaration/)\<`string`, `unknown`\>[] |

### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |
| `options` | [`CommandOptions`](/docs/sdk/index/interfaces/commandoptions/)\<`R`\> |
| `handler` | (`ctx`: [`CommandContext`](/docs/sdk/index/interfaces/commandcontext/)\<`R`\>) => `unknown` |

### Returns

[`Workload`](/docs/sdk/index/interfaces/workload/)

### Example

```ts
export const stats = command("invoices:stats", { resources: [db] }, async (ctx) =>
  ctx.resources.db.one("select count(*)::int as invoices from invoices"),
);
```

