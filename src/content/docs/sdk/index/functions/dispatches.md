---
# GENERATED by scripts/sync-docs.mjs from gmedia/usai@v0.0.13:docs/sdk/index/functions/dispatches.md — do not edit; fix upstream and re-sync.
title: "Function: dispatches()"
slug: "sdk/index/functions/dispatches"
source: "docs/sdk/index/functions/dispatches.md"
---
[@sakaladev/usai](/docs/sdk/) / [index](/docs/sdk/index/) / dispatches



```ts
function dispatches<W extends Workload>(from: W, ...to: Workload[]): W;
```

Record that `from` invokes or dispatches the tasks `to`, so that `usai
graph`, `inspect` and the reference page show the edge. The annotation
is for the graph only: a dispatch to a task that is not listed here
still runs, and a dispatch to a task that does not exist is refused at
runtime (`unknown_task`) whether or not it is listed. Returns `from`
with its own type — a task wrapped here is still a [TypedWorkload](/docs/sdk/index/interfaces/typedworkload/),
so `ctx.tasks.invoke` still resolves its handler's output — so it wraps a
declaration in place. See [QueueHandle.publish](/docs/sdk/index/interfaces/queuehandle/#publish) and
[publishes](/docs/sdk/index/functions/publishes/) for the durable, cross-process equivalent.

## Type Parameters

| Type Parameter |
| ------ |
| `W` *extends* [`Workload`](/docs/sdk/index/interfaces/workload/) |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `from` | `W` |
| ...`to` | [`Workload`](/docs/sdk/index/interfaces/workload/)[] |

## Returns

`W`

