---
# GENERATED by scripts/sync-docs.mjs from gmedia/usai@v0.0.11:docs/sdk/index/interfaces/QueueContext.md — do not edit; fix upstream and re-sync.
title: "Interface: QueueContext<M, R = ResourceDeclaration[]>"
slug: "sdk/index/interfaces/queuecontext"
source: "docs/sdk/index/interfaces/QueueContext.md"
---
[@sakaladev/usai](/docs/sdk/) / [index](/docs/sdk/index/) / QueueContext



The context of one message delivery: the validated `message`, the
`attempt` number and the message `id`, plus [BaseContext](/docs/sdk/index/interfaces/basecontext/).

## Extends

- [`BaseContext`](/docs/sdk/index/interfaces/basecontext/)

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `M` | - |
| `R` | [`ResourceDeclaration`](/docs/sdk/index/interfaces/resourcedeclaration/)[] |

## Properties

| Property | Modifier | Type | Description | Overrides | Inherited from |
| ------ | ------ | ------ | ------ | ------ | ------ |
| <a id="message"></a> `message` | `readonly` | `M` | - | - | - |
| <a id="resources"></a> `resources` | `readonly` | [`ResourcesOf`](/docs/sdk/index/type-aliases/resourcesof/)\<`R`\> | The declared resources by name, as their in-world handles ([PostgresHandle](/docs/sdk/index/interfaces/postgreshandle/), [CacheLocalHandle](/docs/sdk/index/interfaces/cachelocalhandle/), [HttpClientHandle](/docs/sdk/index/interfaces/httpclienthandle/)). Reading an undeclared name throws `resource_not_declared` with the fix. | [`BaseContext`](/docs/sdk/index/interfaces/basecontext/).[`resources`](/docs/sdk/index/interfaces/basecontext/#resources) | - |
| <a id="attempt"></a> `attempt` | `readonly` | `number` | 1-based attempt number. | - | - |
| <a id="id"></a> `id` | `readonly` | `string` | The queue's id for this message (the one `publish` returned). | - | - |
| <a id="requestid"></a> `requestId` | `readonly` | `string` | The request id of the world that published the message — the same value the publisher's response carried as `x-request-id` — on this world's log lines and outbound calls too; empty when the message was published by a cron tick, a command, or `usai queue run`. | - | - |
| <a id="tasks"></a> `tasks` | `readonly` | [`TaskHandle`](/docs/sdk/index/interfaces/taskhandle/) | Start tasks: owned (`invoke`) or transferred (`dispatch`). | - | [`BaseContext`](/docs/sdk/index/interfaces/basecontext/).[`tasks`](/docs/sdk/index/interfaces/basecontext/#tasks) |
| <a id="queue"></a> `queue` | `readonly` | [`QueueHandle`](/docs/sdk/index/interfaces/queuehandle/) | Publish to a queue topic; durable once the insert commits. | - | [`BaseContext`](/docs/sdk/index/interfaces/basecontext/).[`queue`](/docs/sdk/index/interfaces/basecontext/#queue) |
| <a id="signal"></a> `signal` | `readonly` | [`UsaiAbortSignal`](/docs/sdk/index/interfaces/usaiabortsignal/) | Aborts when this world is cancelled. | - | [`BaseContext`](/docs/sdk/index/interfaces/basecontext/).[`signal`](/docs/sdk/index/interfaces/basecontext/#signal) |
| <a id="env"></a> `env` | `readonly` | `Record`\<`string`, `string` \| `number` \| `boolean` \| `undefined`\> | The declared environment, parsed: `env.int()` gives a number, `env.bool()` a boolean, `env.optional(...)` may be undefined. The static type is the union of those; narrow per key, or type it once with `const e = ctx.env as EnvValues<typeof spec>` (the context does not carry the declaration's type). | - | [`BaseContext`](/docs/sdk/index/interfaces/basecontext/).[`env`](/docs/sdk/index/interfaces/basecontext/#env) |
| <a id="log"></a> `log` | `readonly` | `Pick`\<[`ConsoleLike`](/docs/sdk/index/interfaces/consolelike/), `"log"` \| `"debug"` \| `"info"` \| `"warn"` \| `"error"`\> | Structured logging; lines carry the workload, world and request ids and reach the runtime's log (`target: "app"`). A trailing plain object is structured `fields` (`ctx.log.info("paid", { invoiceId })`), the rest is the message. `console.*` is the same — including `log`, which the guest emits at `info` and the type used to omit, so the one call everyone reaches for first was a compile error against a context whose `log` *is* `console`. | - | [`BaseContext`](/docs/sdk/index/interfaces/basecontext/).[`log`](/docs/sdk/index/interfaces/basecontext/#log) |

## Methods

### sleep()

```ts
sleep(duration: string | number): Promise<void>;
```

A timer owned by this world (`"500ms"`, `"2s"`, or milliseconds). It
resolves early when the world is asked to stop, so a service loop can
`await ctx.sleep("1s")` and then check `ctx.signal.aborted`.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `duration` | `string` \| `number` |

#### Returns

`Promise`\<`void`\>

#### Inherited from

[`BaseContext`](/docs/sdk/index/interfaces/basecontext/).[`sleep`](/docs/sdk/index/interfaces/basecontext/#sleep)

