---
# GENERATED by scripts/sync-docs.mjs from gmedia/usai@v0.0.12:docs/sdk/index/interfaces/SocketHandlers.md — do not edit; fix upstream and re-sync.
title: "Interface: SocketHandlers<I, O, R = ResourceDeclaration[], A = unknown, D = undefined, P = Record<string, string>, Q = Record<string, string | string[]>>"
slug: "sdk/index/interfaces/sockethandlers"
source: "docs/sdk/index/interfaces/SocketHandlers.md"
---
[@sakaladev/usai](/docs/sdk/) / [index](/docs/sdk/index/) / SocketHandlers



The three moments of a connection.

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `I` | - |
| `O` | - |
| `R` | [`ResourceDeclaration`](/docs/sdk/index/interfaces/resourcedeclaration/)[] |
| `A` | `unknown` |
| `D` | `undefined` |
| `P` | `Record`\<`string`, `string`\> |
| `Q` | `Record`\<`string`, `string` \| `string`[]\> |

## Methods

### open()?

```ts
optional open(ctx: SocketContext<I, O, R, A, D, P, Q>): unknown;
```

After the upgrade. Note that **a message is not delivered until `open`
returns**: a socket either pushes or converses, not both ([GUIDE §9](/docs/guide/#9-streams-and-websockets)).

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `ctx` | [`SocketContext`](/docs/sdk/index/interfaces/socketcontext/)\<`I`, `O`, `R`, `A`, `D`, `P`, `Q`\> |

#### Returns

`unknown`

***

### message()?

```ts
optional message(ctx: SocketContext<I, O, R, A, D, P, Q>): unknown;
```

Once per incoming message, in order.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `ctx` | [`SocketContext`](/docs/sdk/index/interfaces/socketcontext/)\<`I`, `O`, `R`, `A`, `D`, `P`, `Q`\> |

#### Returns

`unknown`

***

### close()?

```ts
optional close(ctx: SocketContext<I, O, R, A, D, P, Q>): unknown;
```

After the connection closed, however it closed — including when `open`
itself failed, which is how a push loop normally ends (`ctx.send`
rejects with `client_gone` once the client is gone). This is the only
place to release what the connection held.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `ctx` | [`SocketContext`](/docs/sdk/index/interfaces/socketcontext/)\<`I`, `O`, `R`, `A`, `D`, `P`, `Q`\> |

#### Returns

`unknown`

