---
# GENERATED by scripts/sync-docs.mjs from gmedia/usai@v0.0.13:docs/sdk/index/interfaces/StreamOptions.md — do not edit; fix upstream and re-sync.
title: "Interface: StreamOptions<R extends ResourceDeclaration[] = ResourceDeclaration[]>"
slug: "sdk/index/interfaces/streamoptions"
source: "docs/sdk/index/interfaces/StreamOptions.md"
---
[@sakaladev/usai](/docs/sdk/) / [index](/docs/sdk/index/) / StreamOptions



Options for `http.stream`.

## Extends

- `BodylessPolicies`

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `R` *extends* [`ResourceDeclaration`](/docs/sdk/index/interfaces/resourcedeclaration/)[] | [`ResourceDeclaration`](/docs/sdk/index/interfaces/resourcedeclaration/)[] |

## Properties

| Property | Type | Description | Inherited from |
| ------ | ------ | ------ | ------ |
| <a id="method"></a> `method?` | [`Method`](/docs/sdk/index/type-aliases/method/) | Default `GET`. | - |
| <a id="summary"></a> `summary?` | `string` | - | - |
| <a id="description"></a> `description?` | `string` | - | - |
| <a id="auth"></a> `auth?` | [`AuthDeclaration`](/docs/sdk/index/interfaces/authdeclaration/)\<`unknown`, readonly [`ResourceDeclaration`](/docs/sdk/index/interfaces/resourcedeclaration/)\<`string`, `unknown`\>[]\> | - | - |
| <a id="resources"></a> `resources?` | `R` | - | - |
| <a id="params"></a> `params?` | [`AnySchema`](/docs/sdk/index/type-aliases/anyschema/) | - | - |
| <a id="query"></a> `query?` | [`AnySchema`](/docs/sdk/index/type-aliases/anyschema/) | - | - |
| <a id="contenttype"></a> `contentType?` | `string` | The stream's media type: `text/event-stream` (default; an SSE endpoint), `text/csv`, `application/x-ndjson`, … Sets the response `content-type` unless `stream.start({ headers })` says otherwise, and is what the OpenAPI document and the reference say the endpoint streams. | - |
| <a id="responseheaders"></a> `responseHeaders?` | `Partial`\<`Record`\<`number` \| `"*"`, `Record`\<`string`, `string`\>\>\> | Response headers the stream sets (`content-disposition` for a download), documented. | - |
| <a id="operationid"></a> `operationId?` | `string` | The OpenAPI `operationId` (a generated client's method name). | - |
| <a id="events"></a> `events?` | `Record`\<`string`, [`AnySchema`](/docs/sdk/index/type-aliases/anyschema/)\> | The server-sent events this stream emits, by name, with the schema of each `data:` payload: `events: { tick: z.object({ i: z.number() }) }`. `stream.event("tick", data)` validates `data` against it (a mismatch is a `500 event_contract_violation` — the stream ends early and the log says which event), and the OpenAPI document names them as `components.schemas.<OperationId>EventTick` with the event list in the response description, so a client knows what to `addEventListener` for. | - |
| <a id="timeout"></a> `timeout?` | `string` \| `number` | Per-invocation deadline (`"5s"`, `"500ms"`, or milliseconds). For **finite** work the world is cancelled when it passes: an HTTP caller gets 504, an `invoke` rejects with `deadline_exceeded`, a queue message counts as a failed attempt. Undeclared, finite work gets the runtime default of 30 s. For **connection-bound and persistent** work — a stream, a socket, a service — there is **no default**, because one that ended after 30 s would be useless. A deadline you declare is honoured, and it **stops** the world rather than cancelling it: `ctx.signal` aborts, a pending `ctx.sleep` returns, the handler can finish what it is doing, and a socket's connection is closed so its `close` handler runs. It has one second to unwind and is then cancelled, so a handler that never checks `ctx.signal` still stops near the bound you declared. For a stream the client keeps the `200` it already has and the body stops there — with no trailer and no error, so end an export with a sentinel the reader requires. | [`WorkloadPolicies`](/docs/sdk/index/interfaces/workloadpolicies/).[`timeout`](/docs/sdk/index/interfaces/workloadpolicies/#timeout) |
| <a id="concurrency"></a> `concurrency?` | `number` | How many worlds of this workload may run at once. Past the bound the next one is refused, never queued: an HTTP request gets 503 `capacity_exhausted`, `ctx.tasks.invoke`/`dispatch` reject with the same code in the caller's world, a queue consumer simply claims fewer messages (ADR-0012). | [`WorkloadPolicies`](/docs/sdk/index/interfaces/workloadpolicies/).[`concurrency`](/docs/sdk/index/interfaces/workloadpolicies/#concurrency) |

