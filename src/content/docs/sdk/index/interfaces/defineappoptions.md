---
# GENERATED by scripts/sync-docs.mjs from gmedia/usai@v0.0.11:docs/sdk/index/interfaces/DefineAppOptions.md — do not edit; fix upstream and re-sync.
title: "Interface: DefineAppOptions"
slug: "sdk/index/interfaces/defineappoptions"
source: "docs/sdk/index/interfaces/DefineAppOptions.md"
---
[@sakaladev/usai](/docs/sdk/) / [index](/docs/sdk/index/) / DefineAppOptions



Options for [defineApp](/docs/sdk/index/functions/defineapp/).

## Properties

| Property | Type | Description |
| ------ | ------ | ------ |
| <a id="name"></a> `name?` | `string` | Application name: the OpenAPI title and the reference page's heading. |
| <a id="description"></a> `description?` | `string` | One paragraph about the application, shown on the reference page's overview and as `info.description` of the generated OpenAPI document. Plain text (no markup). Not part of the application identity. |
| <a id="headers"></a> `headers?` | `Record`\<`string`, `string`\> | Response headers set on every response of the application's routes (not on `/_usai/*`): the security headers a proxy would otherwise add (`strict-transport-security`, `x-content-type-options`, `content-security-policy` …). A handler's own header of the same name wins. Static values only — anything computed belongs in the handler. |
| <a id="modules"></a> `modules?` | [`ModuleDeclaration`](/docs/sdk/index/interfaces/moduledeclaration/)[] | The modules this application is composed of (`defineModule`). A module brings its own workloads, resources, migrations and seeders, so a larger application lists modules here and workloads nowhere. |
| <a id="workloads"></a> `workloads?` | [`Workload`](/docs/sdk/index/interfaces/workload/)[] | The workloads that do not belong to a module — routes, tasks, cron entries, consumers, services, commands. **A workload exists because this list (or a module's) reaches it through an `import`: the runtime never scans your files**, so a route file nobody imports is not served. |
| <a id="resources"></a> `resources?` | [`ResourceDeclaration`](/docs/sdk/index/interfaces/resourcedeclaration/)\<`string`, `unknown`\>[] | Resources the application opens that no workload declares — rare: a resource named in a workload's `resources: [...]` is already part of the application. Everything here is opened at activation. |
| <a id="env"></a> `env?` | [`EnvDeclaration`](/docs/sdk/index/interfaces/envdeclaration/)\<`Record`\<`string`, [`EnvField`](/docs/sdk/index/interfaces/envfield/)\<`unknown`\>\>\> | The environment the whole application requires (`env({ … })`): every variable, its type and whether it is required. A missing required variable fails activation with all of them named at once, never at the first request. |

