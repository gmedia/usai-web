---
# GENERATED by scripts/sync-docs.mjs from gmedia/usai@v0.0.13:docs/sdk/index/interfaces/StreamContext.md — do not edit; fix upstream and re-sync.
title: "Interface: StreamContext<O extends StreamOptions = StreamOptions>"
slug: "sdk/index/interfaces/streamcontext"
source: "docs/sdk/index/interfaces/StreamContext.md"
---
[@sakaladev/usai](/docs/sdk/) / [index](/docs/sdk/index/) / StreamContext



The context of a streaming request: request facts plus [BaseContext](/docs/sdk/index/interfaces/basecontext/).

## Extends

- [`BaseContext`](/docs/sdk/index/interfaces/basecontext/)

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `O` *extends* [`StreamOptions`](/docs/sdk/index/interfaces/streamoptions/) | [`StreamOptions`](/docs/sdk/index/interfaces/streamoptions/) |

## Properties

| Property | Modifier | Type | Description | Overrides | Inherited from |
| ------ | ------ | ------ | ------ | ------ | ------ |
| <a id="requestid"></a> `requestId` | `readonly` | `string` | The request's id (`x-request-id`, the client's or minted). | - | - |
| <a id="resources"></a> `resources` | `readonly` | [`ResourcesOf`](/docs/sdk/index/type-aliases/resourcesof/)\<`O`\[`"resources"`\]\> & `AuthResourcesOf`\<`O`\[`"auth"`\]\> | The declared resources, typed by name from `resources: [...]`. | [`BaseContext`](/docs/sdk/index/interfaces/basecontext/).[`resources`](/docs/sdk/index/interfaces/basecontext/#resources) | - |
| <a id="method"></a> `method` | `readonly` | [`Method`](/docs/sdk/index/type-aliases/method/) | - | - | - |
| <a id="path"></a> `path` | `readonly` | `string` | - | - | - |
| <a id="url"></a> `url` | `readonly` | `string` | - | - | - |
| <a id="params"></a> `params` | `readonly` | `OutputOf`\<`O`\[`"params"`\], `Record`\<`string`, `string`\>\> | Path parameters, validated against `params` (strings when undeclared). | - | - |
| <a id="query"></a> `query` | `readonly` | `OutputOf`\<`O`\[`"query"`\], `Record`\<`string`, `string` \| `string`[]\>\> | Query, validated against `query`. | - | - |
| <a id="headers"></a> `headers` | `readonly` | `Record`\<`string`, `string`\> | - | - | - |
| <a id="auth"></a> `auth` | `readonly` | `O`\[`"auth"`\] *extends* [`AuthDeclaration`](/docs/sdk/index/interfaces/authdeclaration/)\<`P`, readonly [`ResourceDeclaration`](/docs/sdk/index/interfaces/resourcedeclaration/)\<`string`, `unknown`\>[]\> ? `P` : `undefined` | The principal the `auth` declaration resolved; `undefined` without one. | - | - |
| <a id="tasks"></a> `tasks` | `readonly` | [`TaskHandle`](/docs/sdk/index/interfaces/taskhandle/) | Start tasks: owned (`invoke`) or transferred (`dispatch`). | - | [`BaseContext`](/docs/sdk/index/interfaces/basecontext/).[`tasks`](/docs/sdk/index/interfaces/basecontext/#tasks) |
| <a id="queue"></a> `queue` | `readonly` | [`QueueHandle`](/docs/sdk/index/interfaces/queuehandle/) | Publish to a queue topic; durable once the insert commits. | - | [`BaseContext`](/docs/sdk/index/interfaces/basecontext/).[`queue`](/docs/sdk/index/interfaces/basecontext/#queue) |
| <a id="signal"></a> `signal` | `readonly` | [`UsaiAbortSignal`](/docs/sdk/index/interfaces/usaiabortsignal/) | Aborts when this world is cancelled. | - | [`BaseContext`](/docs/sdk/index/interfaces/basecontext/).[`signal`](/docs/sdk/index/interfaces/basecontext/#signal) |
| <a id="env"></a> `env` | `readonly` | `Record`\<`string`, [`EnvValue`](/docs/sdk/index/type-aliases/envvalue/)\> | The declared environment, parsed: `env.int()` gives a number, `env.bool()` a boolean, `env.list()` an array, `env.optional(...)` may be undefined. A module's declaration is resolved here too. The static type is the union of those; narrow per key, or type it once with `const e = ctx.env as EnvValues<typeof spec>` (the context does not carry the declaration's type). | - | [`BaseContext`](/docs/sdk/index/interfaces/basecontext/).[`env`](/docs/sdk/index/interfaces/basecontext/#env) |
| <a id="log"></a> `log` | `readonly` | `Pick`\<[`ConsoleLike`](/docs/sdk/index/interfaces/consolelike/), `"log"` \| `"debug"` \| `"info"` \| `"warn"` \| `"error"`\> | Structured logging; lines carry the workload, world and request ids and reach the runtime's log (`target: "app"`). A trailing plain object is structured `fields` (`ctx.log.info("paid", { invoiceId })`), the rest is the message. `console.*` is the same — including `log`, which the guest emits at `info` and the type used to omit, so the one call everyone reaches for first was a compile error against a context whose `log` *is* `console`. | - | [`BaseContext`](/docs/sdk/index/interfaces/basecontext/).[`log`](/docs/sdk/index/interfaces/basecontext/#log) |

## Methods

### sleep()

```ts
sleep(duration: string | number): Promise<void>;
```

A timer owned by this world (`"500ms"`, `"2s"`, or milliseconds). It
resolves early when the world is asked to stop, so a service loop can
`await ctx.sleep("1s")` and then check `ctx.signal.aborted`.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `duration` | `string` \| `number` |

#### Returns

`Promise`\<`void`\>

#### Inherited from

[`BaseContext`](/docs/sdk/index/interfaces/basecontext/).[`sleep`](/docs/sdk/index/interfaces/basecontext/#sleep)

