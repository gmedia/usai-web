---
# GENERATED by scripts/sync-docs.mjs from gmedia/usai@v0.0.12:docs/sdk/index/interfaces/BaseContext.md — do not edit; fix upstream and re-sync.
title: "Interface: BaseContext"
slug: "sdk/index/interfaces/basecontext"
source: "docs/sdk/index/interfaces/BaseContext.md"
---
[@sakaladev/usai](/docs/sdk/) / [index](/docs/sdk/index/) / BaseContext



What every handler receives, whatever the workload kind. Everything
asynchronous here is a host operation **owned by this world**: it is
cancelled when the world is, and a finite world may not end while one is
still pending (that is a lifecycle error with a diagnostic, not a leak).
Nothing on the context survives the world.

## Extended by

- [`HttpContext`](/docs/sdk/index/interfaces/httpcontext/)
- [`RawContext`](/docs/sdk/index/interfaces/rawcontext/)
- [`SeederContext`](/docs/sdk/index/interfaces/seedercontext/)
- [`TaskContext`](/docs/sdk/index/interfaces/taskcontext/)
- [`CronContext`](/docs/sdk/index/interfaces/croncontext/)
- [`CommandContext`](/docs/sdk/index/interfaces/commandcontext/)
- [`ServiceContext`](/docs/sdk/index/interfaces/servicecontext/)
- [`StreamContext`](/docs/sdk/index/interfaces/streamcontext/)
- [`SocketContext`](/docs/sdk/index/interfaces/socketcontext/)
- [`QueueContext`](/docs/sdk/index/interfaces/queuecontext/)

## Properties

| Property | Modifier | Type | Description |
| ------ | ------ | ------ | ------ |
| <a id="resources"></a> `resources` | `readonly` | `Record`\<`string`, `unknown`\> | The declared resources by name, as their in-world handles ([PostgresHandle](/docs/sdk/index/interfaces/postgreshandle/), [CacheLocalHandle](/docs/sdk/index/interfaces/cachelocalhandle/), [HttpClientHandle](/docs/sdk/index/interfaces/httpclienthandle/)). Reading an undeclared name throws `resource_not_declared` with the fix. |
| <a id="tasks"></a> `tasks` | `readonly` | [`TaskHandle`](/docs/sdk/index/interfaces/taskhandle/) | Start tasks: owned (`invoke`) or transferred (`dispatch`). |
| <a id="queue"></a> `queue` | `readonly` | [`QueueHandle`](/docs/sdk/index/interfaces/queuehandle/) | Publish to a queue topic; durable once the insert commits. |
| <a id="signal"></a> `signal` | `readonly` | [`UsaiAbortSignal`](/docs/sdk/index/interfaces/usaiabortsignal/) | Aborts when this world is cancelled. |
| <a id="env"></a> `env` | `readonly` | `Record`\<`string`, [`EnvValue`](/docs/sdk/index/type-aliases/envvalue/)\> | The declared environment, parsed: `env.int()` gives a number, `env.bool()` a boolean, `env.list()` an array, `env.optional(...)` may be undefined. A module's declaration is resolved here too. The static type is the union of those; narrow per key, or type it once with `const e = ctx.env as EnvValues<typeof spec>` (the context does not carry the declaration's type). |
| <a id="log"></a> `log` | `readonly` | `Pick`\<[`ConsoleLike`](/docs/sdk/index/interfaces/consolelike/), `"log"` \| `"debug"` \| `"info"` \| `"warn"` \| `"error"`\> | Structured logging; lines carry the workload, world and request ids and reach the runtime's log (`target: "app"`). A trailing plain object is structured `fields` (`ctx.log.info("paid", { invoiceId })`), the rest is the message. `console.*` is the same — including `log`, which the guest emits at `info` and the type used to omit, so the one call everyone reaches for first was a compile error against a context whose `log` *is* `console`. |

## Methods

### sleep()

```ts
sleep(duration: string | number): Promise<void>;
```

A timer owned by this world (`"500ms"`, `"2s"`, or milliseconds). It
resolves early when the world is asked to stop, so a service loop can
`await ctx.sleep("1s")` and then check `ctx.signal.aborted`.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `duration` | `string` \| `number` |

#### Returns

`Promise`\<`void`\>

