---
# GENERATED by scripts/sync-docs.mjs from gmedia/usai@v0.0.9:docs/STATUS.md — do not edit; fix upstream and re-sync.
title: "Status"
slug: "status"
source: "docs/STATUS.md"
---
> Where the project is right now. Update this in the same change that moves it.

**Last updated:** 2026-09-23

## Where things stand

```text
D0–D15                      FIRST IMPLEMENTATION PRESENT (each has acceptance tests)
Product breadth             ESTABLISHED — stop broadening; depth now
Milestone acceptance        AUDITED — docs/ACCEPTANCE-AUDIT.md: all D0–D15 items ✓ or ◐, no ✗
Production substrate        Wasm image + pooling/COW (ADR-0016); attributed and fixed on the research VM
                            (hello 1.01 ms p50 at c=1; 13.6k req/s at c=16 on 16 cores with `usai bench`, no proxy, no co-tenant — the
                            2026-09-18 attribution report; the 2026-09-19 comparative run with the soak as co-tenant saw 5.8k); 1 h `usai bench`
                            soak: 49 M hello requests, 0 errors, RSS +0.9 %
Developer preview           v0.0.5 (2026-09-18) — one tag publishes binaries (linux x86_64/aarch64, macOS arm64), npm
                            (@sakaladev/usai, @sakaladev/create-usai) and Docker images (runtime + dev, amd64 + arm64);
                            main since then: P7 fixes, the documentation split, typed ctx.resources, replica flags (0.0.6 candidate)
Efficiency envelope         RE-MEASURED 2026-09-23 on a box charged for its own page cache: supported floor 192 MiB /
                            1 vCPU (128 MiB serves the same shape with zero reclaim, no room for a held revision);
                            technical floor 64 MiB, NOT the 48 MiB published before — at 48 the box thrashes on its own
                            text and serves 1 req/s without ever being killed. Density unchanged: ≈30 MiB PSS and
                            0.00 % CPU per idle application at N=50 (Node + Fastify: ≈36 MiB, 0.12 %). Comparator
                            floors still owed (docs/measurements/2026-09-23-floor-accounting.md)
Production ready            NOT YET — alpha, production qualification in progress: 24 h soak clean (35.0 M requests, 0 runtime
                            errors), connection campaign passed, 72 h soak PASSED (73.6 M requests, 0 × 5xx, 0 quarantined, flat RSS); see below
```

## Production readiness (honest report, 2026-09-20)

Usai is an **alpha with production qualification in progress**: no longer a
preview (the 24 h soak, the failure campaigns and the connection campaign
passed on a production-shaped deployment), not yet a support promise (one
external developer has used it, not the three to ten P7 asks for, and
**contracts still move within 0.0.x** — [`docs/CONTRACT-FREEZE.md`](/docs/contract-freeze/) is the
inventory that decision needs). Run it for development, evaluation and
internal tools you can afford to restart; put it in front of paying traffic
only if you can live with those three caveats. The gates in [`docs/ROADMAP.md`](/docs/roadmap/), with where each stands:

| Gate | State | What exists / what is missing |
|---|---|---|
| Correctness of the lifecycle model | ✓ automated | C1–C18 have acceptance tests; malformed artifacts, budget exhaustion, forced shutdown, revision replacement under load are tested; 1 h soak on a VM: 49 M requests, 0 errors, flat RSS |
| Distribution (P3.5) | ✓ | native binary, `pnpm usai` wrapper, Docker runtime/dev images, compose path, artifact↔runtime compatibility refused before serving, smoke test in CI and after every release |
| Real application built as a user (P4) | ✓ | `examples/invoicing` (tenants, sessions, transactional invoices, pagination, signed retried webhooks over outbound HTTP, cron, command, seeder, test through the real runtime) — [GUIDE §17](/docs/guide/#17-the-p4-application-examplesinvoicing); primitives it forced: ADR-0017, enum params. A second outside-developer run (notes API) fed 14 fixes |
| Operational qualification (P5) | ✓ | production-shaped deployment (Caddy → runtime image → PostgreSQL) broken 13 ways under load; `docs/runbooks/` (15 pages, plus the index: which metric, which log line, what workloads do, when it recovers); findings fixed (503 for unavailable dependencies, migrations in the artifact, bounded revisions, self-retiring revisions); evidence [`docs/measurements/2026-09-18-p5-p6-qualification.md`](/docs/measurements/2026-09-18-p5-p6-qualification/) |
| Reliability qualification (P6) | ✓ | 1 000 revision replacements under load (2.04 M requests, 0 errors, flat RSS after the malloc-arena fix), DB flap ×10, restart loop ×10, overload (only 503 capacity), dead-letter — passed; **24 h soak passed** (2026-09-18 22:18 → 09-19 22:19 UTC: 34 978 737 ok, 0 × 4xx/5xx/503, 35.0 M worlds, RSS 54.8 → 56.2 MiB, 0 detached work, 0 quarantined; 9 client timeouts in 5 seconds traced to two host stalls while Docker extracted image layers for the benchmarks — `journalctl -u docker` at the same seconds); **72 h soak passed** (2026-09-19 22:28 → 09-22 22:28 UTC: 73 627 701 ok, 0 × 5xx/503, 73.6 M worlds, RSS 51.5 → 53.5 MiB, 0 detached work, 0 quarantined, no runtime WARN or ERROR in three days; 36 client timeouts in 15 seconds in two host-stall episodes; the run's throughput decays 794 → 154 req/s with a flat p50 and a flat per-request CPU — the 7.4 M invoices it wrote, not the runtime); **connection campaign passed** (500 SSE+WS cycles; 40 connections held through a revision replacement, an app restart and a proxy restart; abrupt client death costs slots until `USAI_SOCKET_IDLE_TIMEOUT` — the sizing rule is now written down; unread streams +0.7 MiB each; idle sockets closed 1008); **two-replica campaign passed** (2026-09-20, beside the soak on cpus 12–15: HTTP and queue shared 50/50, concurrent migrations, cron on one, rolling restart 0 × 502, one replica `kill -9` with messages in flight — two bugs found and fixed: a stop closed the listener before the balancer could learn (one 502 per restart → `--drain-grace`), and messages a dead replica had claimed stayed `processing` forever → lost-consumer reclaim); **three-replica campaign passed** (2026-09-23, on the published 0.0.8 images beside the 24 h soak: 11 of 11 — even round robin across three, the queue claim still exactly-once across three consumers, three concurrent migrators applying each file once, a rolling restart of all three under load at 0 × 5xx. The run before it reported four PASSes that were worthless — the tenant token was empty and all 1.5 M requests were 401s, which every "no 5xx" rule accepts; a load verdict now has to prove something was served). Evidence: [`docs/measurements/2026-09-18-p5-p6-qualification.md`](/docs/measurements/2026-09-18-p5-p6-qualification/) |
| External developer validation (P7) | ◐ | **one external developer so far** (2026-09-23, informal — an Express developer, a friend of the maintainer, reported secondhand; see the bullet in Known gaps) plus four fresh-eyes runs (0.0.1: 21 items; 0.0.4: 14; **0.0.5: three personas — payment webhooks, a Rails developer's multi-tenant SaaS, an SRE deploying from the public docs — 0 blockers on the developer side, 2 on the operator side, ~25 frictions, all fixed below**; then, unreleased: an Express migrator, an adoption memo from the docs alone, an SRE runbook drill, and a first-production-deployment operator round on 0.0.8 whose two serious findings were both deployment guidance at the proxy boundary — every runtime finding fixed the same day, see Known gaps); doc-to-first-200 in 4–10 min for all three; no human outside developers yet |
| Frozen contracts, [`SUPPORTED.md`](/docs/supported/), upgrade matrix (RC) | ◐ | [`SUPPORTED.md`](/docs/supported/) written; runtime × artifact (N, N−1) matrix tested in CI against the last release; contracts still move within 0.0.x (they must, until P7 says the API is right) |
| Supply chain | ✓ | artifact signing decided and documented (**ADR-0020**; `usai keygen` / `build --sign` / `run --require-signature`, native image covered, control installs verified, and the threat campaign proves a signed artifact runs while a tampered one and another key's are refused); RustSec + npm audit + a secret scan in CI; binaries with SHA-256 **and Sigstore build provenance**, npm via Trusted Publishing with `--provenance`, images by digest with provenance pushed beside the manifest |
| Security qualification | ◐ | threat model, [`SECURITY.md`](/docs/security/); robustness tests (600 mutated artifacts, 600 garbage control/HTTP requests: no panic, no leaked work); worlds are semantic isolation, not a hostile sandbox (ADR-0008); coverage-guided fuzzing (`fuzz/`, libFuzzer: manifest → definition → OpenAPI/router/cron/env, the HTTP boundary and guest output, source maps, **the host side of the guest bridge and the JSON → wire-type conversion of every SQL parameter** — the two blind spots the fuzzing report named, added 2026-09-23) two minutes per target on every push and ten nightly; **the threat model is verified rather than asserted** (`scripts/qualification/threat/run.sh`: 39 probes over body bounds, validation before a world, deadlines and the CPU watchdog, a world that allocates 4 GiB, error sanitization, what reaches a log line, secrets in the manifest, the operator tokens, switched-off surfaces, the control surface's public-bind refusal, the WebSocket idle close, that nothing under `/_usai/` exists on the application listener, and artifact signatures — all passing, in the release gate); **secrets are scanned on every push**. Still ◐: the corpus is days old rather than months, and the QuickJS core itself and the PostgreSQL wire protocol stay third-party surfaces |
| Comparative benchmarks | ✓ (first run) | hello endpoint vs Node/Bun/Deno on the VM: [`docs/measurements/2026-09-19-comparative-hello.md`](/docs/measurements/2026-09-19-comparative-hello/) — ≈1 ms per request is the **measured fixed cost of the current implementation** (2× below Node, 5× below Bun/Deno at c=64), not a proven price of the model: the research representation reached +51 µs over Node/Fastify at c=1 on HTTP+PostgreSQL. **P8 (hot-path parity)** 2026-09-19, c=1 phase done — [`docs/measurements/2026-09-19-p8-parity.md`](/docs/measurements/2026-09-19-p8-parity/): the whole-request invoice (`USAI_PROFILE=1`, host phases + engine phases + the guest's own ledger) found three costs the semantics never asked for — Zod's accepting path rebuilt per world, three evals per request, validation paid twice — and four levers removed them: **C2a** structural-sample warm-up in the snapshot, **C1** direct-call guest ABI (ADR-0018), **C2b** validate once (exact finalizer), **C3** owned buffers + in-core settle (four typed calls per request) after a `perf` audit that put the runtime's own per-request logic at ≈2.5 %. VM 47, c=1 pinned, soak co-tenant: hello 1.70 → 0.90 ms p50 / 1.45 → 0.65 ms CPU; contract-heavy 5.27 → 1.28 / 4.76 → 0.90; PostgreSQL classes −18…45 % CPU. What remains is itemised (interpreter running the developer surface ≈0.26 ms, boundary JSON ≈0.15, fresh world ≈0.16, Wasmtime lazy funcref init ≈2 % — upstream). `suite.sh leak` (counter/cancellation/RSS, all comparators) and the PHP-FPM comparator at c=1 measured the same morning. Next: `suite.sh sweep` on the idle VM after the 72 h soak |
| Efficiency envelope (P8E) | ✓ ours, ◐ comparators | **Density stands** (bare processes on the host, where the host is the payer): ≈30 MiB PSS and 0.00 % CPU per idle application at N=50 vs ≈36 / 0.12 % for Node + Fastify. **Both floors were void and are re-measured** ([`docs/measurements/2026-09-23-floor-accounting.md`](/docs/measurements/2026-09-23-floor-accounting/)): the old cells bind-mounted the binary they measured and page cache is charged to whoever faults it in first, so the box was never billed for the runtime's own text — a comparator cell held 88 MiB inside a 48 MiB box and was never killed. Cells now drop the page cache first, record what the kernel charged, and use the binary that ships. Re-measured: **supported floor 192 MiB / 1 vCPU** (the production shape holds 106 MiB resident / 93 MiB charged, identical at 128/192/256 MiB boxes, ≈1 100 req/s at c=16, p50 14 ms, **zero ceiling hits at 128 MiB**; 192 covers the held revision a rolling deployment carries), **technical floor 64 MiB — not 48**: at 48 MiB the box is charged to its ceiling, reclaims its own text 1.4 M times and serves 1 req/s, never killed. Still ◐: the comparator floors need an idle host (the soak's load generator maps the same `node` binary a cell would evict) and run after it. Original report: [`docs/measurements/2026-09-20-p8e-efficiency.md`](/docs/measurements/2026-09-20-p8e-efficiency/) (VM 47, cpus 12–15, soak as disclosed co-tenant, 72 cells + the C2 cells): **technical floor ~~48 MiB / 0.25 vCPU~~ 64 MiB / 1 vCPU** (hello; 32 MiB OOM-killed at the first request), **supported floor 192 MiB / 1 vCPU / 48 worlds** (PostgreSQL pool 4, task, cron, status + metrics: 1 015 req/s at c=16, p50 15 ms, no OOM in any of 30 cells down to 128 MiB; 8-world cells answer c=16 with 503 `capacity_exhausted` and 0 × 5xx — admission, not failure), **density** N=1…50 vs Node + Fastify: ≈30 MiB PSS / 47 RSS and 0.00 % CPU per idle Usai application at N=50 vs ≈36 / 84 and 0.12 %; cold first response 11–16 ms vs 45–62; burst 100 req/s costs Usai ≈1.7× Node's CPU per request. Idle fix: the watchdog and epoch tickers park while no world is live (`crate::idle`, 300 wakeups/s → 0); `/_usai/status` reports `process` (RSS, PSS, faults, CPU, threads, fds). Against the prediction: memory does not return after a burst (≈4 MiB RSS / 1.5 PSS per touched slot, kept resident) — C2 measured: `USAI_WASM_KEEP_RESIDENT` 2 MiB is identical to the default, 0 returns the memory (100 → 45 MiB) at half the throughput and ≈100 faults per request; default kept, knob documented with its price. [`SUPPORTED.md`](/docs/supported/) Host envelope row, Q17 (multi-application process) recorded |
| Vendored Wasmtime patch | ◐ → ✓ upstream | **[bytecodealliance/wasmtime#14357](https://github.com/bytecodealliance/wasmtime/pull/14357) merged into `main` 2026-09-23** (`a2e2d86`, by a Wasmtime maintainer; the patch plus the unit tests that fail on the unpatched code). The vendored file is byte-identical to upstream `main`. v49.0.0 (2026-09-21) predates the merge, so the vendor stays until a release carries it — then re-vendor that version and drop `vendor/`. Provenance in `vendor/README.md`, the story in [`docs/upstream/wasmtime-pagemap-reset.md`](/docs/upstream/wasmtime-pagemap-reset/); `engine::wasm::tests::a_reused_slot_is_fresh_*` are the rebase tests |

Next gate: **P7**, outside developers building from the public
docs alone (three real humans, unguided, would already be informative); RC freezes contracts only after P7 says the API is right.
**The concurrency sweep is in** (2026-09-23, idle VM: [`docs/measurements/2026-09-23-sweep.md`](/docs/measurements/2026-09-23-sweep/)) — the third number the P9 decision needed. One Usai process beats one Node process from c=8 up on every class (hello 14.8k vs 12.7k, DB read 7.0k vs 6.3k, transaction 4.2k vs 2.6k at c=64), sits 2× behind an eight-worker Node cluster on the database classes and 7× behind it on hello, and pays 6–14× Node's CPU per trivial request and ≈0.5 GiB for 64 concurrent worlds. The PHP stack was re-run the same day after three fixes of ours (nginx had no keepalive upstream and ran out of ephemeral ports; the Laravel image did not build; the Laravel controller declared `user` twice): **Usai is ahead of bare tuned PHP-FPM on every database class** (+19 % read, +54 % transaction, +80 % auth+read at c=64), level on the insert, behind 32 % on pure validation — and **ahead of Laravel 12 on every class by 1.8× to 7×, at a third to a seventh of its CPU per request**. The queue campaign on the idle host and the comparator floors run after it.

Multi-instance: the topology is stated ([`SUPPORTED.md`](/docs/supported/)) and campaigned with two replicas (2026-09-20): HTTP and queues share work, migrations serialize on an advisory lock, **cron ticks on every instance that schedules** (`--no-cron` elsewhere), rolling restarts lose nothing with the drain grace and a proxy that health-checks `/_usai/ready`.

Current phase ([`docs/ROADMAP.md`](/docs/roadmap/)): **P5 done, P6 done (72 h soak passed), P7 in progress, P8 c=1 done, P8E done; the sweep is in and P9's scope is decided (ADR-0019).** Depth, not breadth.

```text
execution substrate recovery   ← ADR-0016 first pass done; measure on a real VM next
end-to-end correctness audit
realistic application
performance / soak / multi-core
alpha
```

## Measured — substrate (2026-09-18, release; engineering numbers)

Full ledger in [`docs/measurements/2026-09-18-execution-path-attribution.md`](/docs/measurements/2026-09-18-execution-path-attribution/)
(research VM, 16 × Xeon E5-2680 v4). After P1 attribution and P2 fixes:

```text
hello, usai bench, VM          P1 (aded35c)              P2 (ff2688f)
c=1                            369 req/s, p50 2.62 ms     658 req/s, p50 1.52 ms
c=16                           2 497 req/s, p50 6.24     9 763 req/s, p50 1.58
c=64                           2 471 req/s, p50 25.6     9 788 req/s, p50 6.45
minor faults / request         95–315                    0
```

What P1 found and P2 fixed: zod's lazy schema initialization was paid by
every fresh world (1.64 ms → prepared before the snapshot, callback-free);
Wasmtime's slot reset stopped its dirty-page scan at 32 regions and
decommitted the rest, so every world refaulted its heap and the
`madvise`/`mprotect` storm capped multi-core scaling with TLB-shootdown IPIs
(vendored one-line patch, 0 faults, ×3.9 at c=16); zero-delay timers cost a
1 ms timer-wheel tick (yield); the watchdog handshake cost 4 context switches
per request (atomic slots + ticker). The world's remaining 1.05 ms on the VM
is accounted: 0.46 validation + handler, 0.24 eval floor, 0.13 reset, 0.18
bookkeeping, 0.02 create.

## Done

- **D0 foundation.** Rust workspace (`crates/usai-runtime`, `crates/usai-cli`), pnpm workspace (`packages/usai`, `packages/create-usai`, `examples/hello`, test fixtures), `make check` (fmt, clippy `-D warnings`, cargo test, tsc, node --test), GitHub Actions CI, pinned Rust 1.98.1 / Node 24 / pnpm 12.
- **D1 lifecycle core** (`usai-runtime`): immutable `ApplicationDefinition`; engine boundary + QuickJS-ng substrate (ADR-0015) + guest bridge ABI ([`docs/GUEST-ABI.md`](/docs/guest-abi/)); `WorldDriver` with one identity-first completion gate; bounded ownership `Ledger` + gauges; host operations with independent owners; `ResourceManager`/`ResourceIdentity`/`cache.local`; hierarchical `Budget`; revisions `installed → active → draining → retired`. 17 acceptance tests in `tests/lifecycle.rs`.
- **D2 HTTP contract workload.** `usai` SDK (`defineApp`, `defineModule`, `http.*`, `http.raw`, `auth.*`, `errors`, `env`, `cache.local`, `task`/`cron`/`command`/`service` declarations, Standard Schema interop, manifest `describe`, in-world dispatcher). Runtime HTTP host: `matchit` router built once per revision, decode, JSON Schema validation **before** world creation (Zod 4 describes itself via `~standard.jsonSchema`), scalar coercion for string transports, admission, world, response contract, error mapping (C11), raw escape hatch, client-disconnect cancellation, deadline → 504. Build pipeline: esbuild via the SDK's `build/bundle.mjs` + manifest extraction in a capability-less world (ADR-0009). 13 acceptance tests in `tests/http.rs` against a real server and the real SDK.
- **D3 developer loop.** `usai build`, `usai run`, `usai dev` (watch → rebuild → install → activate → drain previous; failed builds keep the previous revision serving), `usai inspect` (+`--json`), `usai config` (effective values with sources; `usai.config.ts` evaluated declaratively). `examples/hello` runs.
- **D4 tasks + explicit ownership transfer.** `ctx.tasks.invoke` = owned child world (cancelled with the parent, outcome returned as a contract); `ctx.tasks.dispatch` = ownership transferred to the runtime-owned, bounded, non-durable `TaskQueue` (ADR-0010; losses at shutdown are counted). Child records on `WorkResult`; dispatch is not detached work; draining waits for dispatched tasks; parent globals invisible in the child world.
- **D5 cron + commands.** Per-revision cron schedulers (croner, 5/6-field), fresh world per tick, overlap `skip`/`allow`, invalid schedules fail at install, schedulers stop at drain; `RuntimeConfig.cron_scheduler` for instances that must not tick. `Runtime::run_cron` / `run_command` / `run_task` for deterministic invocation; CLI `usai cron run`, `usai app <cmd> [args]`, `usai task run --input`. 9 acceptance tests in `tests/workloads.rs`.
- **D6 PostgreSQL capability.** `resource/postgres.rs` (tokio-postgres + deadpool): pool at runtime lifetime, one lease per operation, prepared statements cached per physical connection, JSON↔SQL typing by prepared-statement parameter types (int/float/numeric/bool/text/uuid/json/timestamps/date/arrays) and by column types. C5 paths: normal/SQL error → terminal → return; cooperative cancel → `CancelRequest` → await the original query's 57014 → return; abandonment without terminal proof → quarantine (removed from pool); session state reset on checkout (`pool.recycling: clean` default). Unreachable database fails activation. SDK `postgres("main", { urlEnv, pool })` + `query/one/execute`. 8 acceptance tests in `tests/postgres.rs` against a real server (`USAI_TEST_DATABASE_URL`, else a portable PostgreSQL 18 the tests download to `~/.cache/usai/postgresql`). `examples/postgres`.
- **D7 project model.** Module contributions (`migrations:`/`seeders:` globs, resources declared by several modules dedupe when identical and error when they conflict); migration discovery from `usai.config.ts` includes + module globs (root-relative, ordered by file name); `usai db migrate` applies each SQL file in its own transaction on a leased connection with a `usai_migrations` ledger row in the same transaction, refuses tampered applied files, never runs at startup; `usai db status`; seeders are `seeder(...)` default exports discovered by glob and run by `usai db seed [name]` as `command:seed:<name>` in a synthetic definition that borrows the app's resources and env; typed env validated for shape at activation (url/int/bool/enum) and delivered typed in `ctx.env`; `execute()` always injects the revision env. Fixture `tests/fixtures/project-app` uses colocated and centralized layouts at once. 3 acceptance tests in `tests/project.rs`.
- **D8 API metadata + OpenAPI.** `openapi.rs` generates OpenAPI 3.1 from the ApplicationDefinition: paths from HTTP triggers, path/query/header parameters from the extracted JSON Schemas, request bodies, responses by declared status, declared errors and the standard error envelope, security schemes from auth declarations, module names as tags; raw endpoints are opaque (`x-usai-raw`), in-world-only contracts are flagged (`x-usai-validated-in-world`). `usai generate openapi [--out]`; the dev server serves `/_usai/openapi.json` and a dependency-free `/_usai/docs`. 1 acceptance test (served document == generated document; not served on a production host).
- **D9 service workload.** `service("name", handler)` starts one persistent world per revision at activation (`workloads/services.rs` supervisor); state persists because the service is alive; finite worlds cannot see it. Drain stops services first: graceful (`__usai.stop` — `ctx.signal` fires, pending `sleep`s resolve, the loop exits and the handler's return is the service's outcome), hard cancel after `drain_timeout`. Service state in `RuntimeStatus.revisions[].services` and the `usai dev` banner. restart policy: D13. 1 acceptance test.
- **D10 queue / message workload.** `queue.consume("topic", { message, concurrency, retry, database }, handler)` + `ctx.queue.publish(topic, message, { delayMs })`. the workload interface is substrate-independent (topic, message contract, concurrency, retry); the v0 *substrate* is a PostgreSQL table (`usai_queue`) claimed with `FOR UPDATE SKIP LOCKED` — persistent consumer loops per revision (`workloads/queue.rs`), fresh world per message, concurrency from the declaration, message JSON Schema validated before the world exists (invalid → dead, no world), explicit retry with fixed/exponential backoff and a dead-letter state (ADR-0014; delivery is at-least-once and says so), consumers stop at drain; `RuntimeConfig.queue_consumers`. 1 acceptance test (publish from HTTP world → four messages processed once, retry-then-success across three fresh worlds, dead letter, invalid message never gets a world).
- **D11 WebSocket + stream workloads.** Streams: `http.stream(path, { params, query, auth }, (ctx, stream) => …)` — the response commits at the first `stream.start`/`send`/`event` (SSE helper) and the body ends when the handler returns (`headers sent != work complete`); a handler that never sends is an ordinary response; a client disconnect cancels the world through the body's drop guard; drain stops the loop gracefully. Sockets: `socket(path, { incoming, outgoing }, { open, message, close })` — HTTP upgrade (hyper `with_upgrades` + tungstenite), one world per connection, frames delivered as host completions (`socket.recv`), `ctx.state` connection-local, contract violations reported to the client without closing, application or client close runs `close`, drain sends 1012 and runs `close`. Per-revision `connections_stop`. Server rewritten with per-connection tasks + `TaskTracker` drain. 5 acceptance tests in `tests/connection.rs`.
- **D12 observability + graph.** `observability.rs`: per-world trace record at `debug` (world, workload, revision, termination, outcome, duration, completions, children, violations) with a disabled fast path (`tracing::enabled!` short-circuit) — implemented **and measured** (`USAI_PROFILE_TRACING` in `tests/profile_matrix.rs`: 0.265 ms per empty world with tracing off against 0.279 at info and debug and 0.286 at trace, so the disabled path costs about 5 %); HTTP class-level counters (2xx/3xx/4xx/5xx, rejected-before-world, upgrades, streams); `/_usai/status` (runtime status JSON incl. gauges, revisions, services, tasks, resources, http) and `/_usai/metrics` (Prometheus text) as runtime-owned surfaces (`HttpConfig.serve_status`; `usai run --status`, on in `usai dev`); `usai graph` renders workload → resource [lease] / dispatch → task edges from the definition; `--log-format json`. 1 acceptance test + 1 unit test.
- **D13 hardening (first pass).** Service restart policy (`restart: { mode: never|on-failure|always, backoffMs, maxRestarts }`, doubling backoff, stop always wins). Graceful shutdown drains with a bound; a second SIGINT forces exit and reports live worlds/ops. `usai bench` (engineering measurement, prints percentiles, req/s, RSS high-water, and asserts ownership returned to baseline). Connection-loss SQLSTATEs (`08*`, `57P0x`) quarantine the connection instead of returning it. [`docs/THREAT-MODEL.md`](/docs/threat-model/). 6 tests in `tests/hardening.rs`: memory limit faults cleanly and the runtime continues; failing service restarts per policy then settles; revision replacement under 8 concurrent clients loses no request; tampered/unsupported/missing artifacts are refused with clear errors; budget exhaustion refuses at once (503) and recovers; killed PostgreSQL backend → quarantine → recovery on a replacement connection.
- **D14 developer preview groundwork.** [`docs/GUIDE.md`](/docs/guide/) (install → concepts → every workload kind → resources → operate); `create-usai <dir>` scaffolds a runnable project from a template (tested). Not yet an alpha: see Next.
- **D15 control surface.** `usai run --control 127.0.0.1:3900` serves a generic JSON API (`control.rs`): `GET /health`, `GET /status`, `GET /revisions`, `POST /revisions {artifact}` (install from an artifact directory), `POST /revisions/{id}/activate`, `POST /revisions/{id}/drain`, `DELETE /revisions/{id}` (installed/retired only), `POST /stop`. Bearer token from `USAI_CONTROL_TOKEN`; binding off loopback without a token is refused. `Runtime::remove`. 2 tests. Sakala is one client of this protocol, never a dependency.
- **`usai/test` harness** (`GOAL.md` §36): `testApp({ root })` spawns `usai run --port 0 --control 127.0.0.1:0 --announce`, drives HTTP, and invokes tasks / cron ticks / commands deterministically through the control surface's `POST /invoke`. Tested against `examples/hello` with the repository binary.
- `usai test` runs the project's `node --test` files with `USAI_BIN` set to the running binary and the `usai` export condition (workspace checkouts need no `dist/`). OpenAPI describes streams (`x-usai-stream`) and sockets (`x-usai-socket`, 101/426) explicitly. WebSocket idle timeout (`HttpConfig.socket_idle_timeout`, 300 s default, close 1008; tested). ADR-0015 records the measured per-world numbers and states that its revisit trigger has fired.
- Measured bundle composition (release): SDK only 1.0 ms/world, `zod/mini` 1.3 ms/world, `zod` 6.6 ms/world (`tests/profile_bundles.rs`). `zod/mini` lacks Standard JSON Schema, so before-world validation and OpenAPI degrade with it; documented in the guide.
- **Precompiled image in the artifact** (`cache/image.cwasm` + `cache/image.json`, ADR-0005 addendum): install loads in ~15 ms instead of compiling for seconds on every core; ignored on any digest/engine/fingerprint mismatch; tested (match, corruption, other code).
- **Documentation, three ways (2026-09-19).** `/_usai/docs` rewritten as the **Application Reference** (`crates/usai-runtime/src/docs.html`, one file, offline, 90 KB): one page per HTTP operation, stream, WebSocket, task, cron, queue consumer, service, command, resource, environment and schema; the lifecycle strip as the centrepiece with every item a link (resource → who leases it, task → who hands off, topic → who consumes); ⌘K search over the whole application, `[`/`]` paging, deep links, light/dark/system; a request panel (sticky third column ≥ 1280 px, slide-over below) that sends real requests, remembers one auth value per scheme for the tab, and reports `x-usai-server-ms`; cURL/JavaScript/Python snippets from the contracts; stacked tables at phone width. OpenAPI gained `info.description` (`defineApp({ description })`) and a **public profile** (`usai generate openapi --public`, `/_usai/openapi.json?profile=public`): the consumer contract without `x-usai-*` or the inventory, declared error codes folded into response descriptions. The **SDK Reference** (`docs/sdk/`, 119 pages) is generated by TypeDoc from a doc-comment pass over every public export of `@sakaladev/usai`, `/test` and `/config` (signature, semantics, lifetime and durability, example); `make docs` regenerates, `make docs-check` fails CI when stale; the option types are now exported. Verified in a headless browser at 1440/1100/390 px, light and dark, with a real signup → token → `POST /invoices` through the panel; contrast ≥ 5.3:1 on every pair. A docs-only fresh-eyes run (a developer allowed nothing but `docs/sdk/` and `/_usai/docs`, building two endpoints and a task in `examples/invoicing`) shipped the feature and reported 28 frictions; the substantive ones are fixed: **`ctx.resources` is now typed from `resources: [...]`** (`ResourcesOf`; the SDK examples compile as written), `summary`/`description` on every workload and `description` on auth schemes reach the reference and OpenAPI, route precedence and queue-publish defaults are documented, the OpenAPI document uses reason phrases, omits content on 204, strips safe-integer bounds, lists 503/504, and names the runtime's error codes; the SDK reference has a glossary (world, commit, lease, hand-off, revision) and a version stamp. A second run on main (2026-09-19, scaffold → bookmarks service with Postgres, httpClient, auth, task, cron, SSE, tests; three deliberate mistakes) shipped the feature with typed resources on the first try and reported 11 frictions; fixed: `/_usai/status`/metrics/banner say whether the instance schedules cron, `.env` edits alone activate a new revision in `usai dev`, SSE responses are `text/event-stream` in OpenAPI, the SDK stamps its guest ABI and the runtime refuses a mismatch at install (the symptom had been a fault on every request), `usai run --diagnostics` and `TestResponse.violations`, the scaffold's `.env.example`, the `env` constructors table. A third run on the P8 main (2026-09-19 evening: scaffold → "shelf" reading-list service — PostgreSQL migration, transaction with `for update`, header auth against a table, dispatch vs invoke, cron, seven `usai/test` tests, build + `run --artifact`, two deliberate mistakes) reached the first 200 one minute after scaffolding, needed no source, hit **no blocker** and reported 15 frictions, none a runtime bug; fixed: `usai dev` no longer exits when the first activation fails on a missing variable — it serves 503 `no_active_revision`, names `.env`, and **watches the project root so creating `.env` activates without a source change** (before, only files under the application directory were watched); application log lines carry `workload` and, in JSON, `target: "app"`; one-shot commands no longer echo each log line twice, and colour is off when stderr is not a terminal; `/_usai/status` names its counters (`rejections` by reason, per-workload `2xx…5xx`, latency buckets with their bounds under `le`); `usai inspect` groups workloads by kind whatever the declaration order; the graph edge reads `hands work to →` (it covers `invoke` too); GUIDE states the validate → world → resolver order and its consequence (a bad body is `400` before `401`), that `detached_work` is a `500` only for an operation in flight (a live timer is cancelled and the returned response committed), that timestamps arrive ISO-8601 (the todos example had taught `::text`, which yields PostgreSQL's form), the boundary/in-world message difference, the default migration globs, and the sorted response keys. A fourth run (PHP/Laravel persona: signed raw webhook → durable queue → transaction → dispatched merchant notification over the outbound-HTTP resource → SSE and WebSocket live status; retries, dead-lettering, SIGTERM drain with a socket and a stream open) worked end to end on the first run and matched every documented promise (1012 on drain, `http_connect`/`http_timeout` as 503s, dead-letter after `maxAttempts`); 15 frictions, two real bugs fixed: **a WebSocket upgrade (101) was counted as a 5xx response** in `/_usai/status` and the metrics and the upgrade/stream counters never moved (anyone alerting on 5xx rate would have been paged by normal socket traffic; now 101 is a success and an upgrade, a committed stream a stream), and **`responses: {…}` on `http.raw` never reached the manifest** (the SDK dropped it; and the runtime could not have parsed its string keys inside the tagged trigger enum) — it now shows in `inspect` and OpenAPI. Also: a `publishes()` topic with no consumer in the application is a WARN at activation and marked in `inspect` (a typo was a silent sink); `app.queue(topic).deliver(message)` in the test harness and `usai queue run <topic> --message` deliver one message to a consumer directly (the idempotency test); one-shot commands and `db status` no longer start queue consumers (that was the source of three quarantine WARNs per `db status`); the queue log field `message=` (which tracing treats as the event text) became `id=`; sockets are 101/426 in OpenAPI, not raw-endpoint boilerplate; `RawContext.request` methods documented as methods (`RawRequestBody`); [GUIDE §8](/docs/guide/#8-queue) gained the three things a queue user from another stack must know (every runtime on the database is a consumer — a dev server eats your tests' messages; `dispatch` is not a queue; retention is yours) and the idempotency idiom; [GUIDE §11](/docs/guide/#11-outbound-http) `errors.badGateway` → `errors.unavailable`; the harness names the missing-environment case; the scaffold's README says to replace `hello.test.ts`. A fifth run (Go/Python platform persona: two modules, a `service()` with a deliberate crash and a restart policy, `command()`s via `usai app`, seeders, `cache.local` scoping, a task concurrency limit, `usai config`, signing end to end, a deploy review without docker) found the **one-shot blocker**: `usai app`, `usai task run`, `usai db migrate|status|seed` activated the revision and started every `service()` — a `db migrate` job would have run the ingest loop against production and raced the replicas; fixed with `RuntimeConfig.services` (off in every one-shot) and `usai run --no-services` (`USAI_NO_SERVICES`), reported in `/_usai/status`, the `usai_scheduler` gauge and the banner (a one-shot `catalog:stats` went from 2.2 s to 0.24 s). Also: a handler that cannot start an operation now answers the guest by name (`capacity_exhausted` 503 for a full task slot, `unknown_task`, `unknown_operation`) instead of a bare `op_refused` (the documented code was right, the runtime was wrong); a service that exhausts its restart policy logs `service gave up` and the service lines no longer leak `Some(WorldId(1))`; a module migration/seeder glob matching no file is a WARN (globs are root-relative); seeders print their return value; `--drain-timeout` / `USAI_DRAIN_TIMEOUT` (the server's connection bound follows it); `usai probe live|ready` for container healthchecks without curl (the production compose uses it); docs: what runs where ([GUIDE §14](/docs/guide/#14-operate), SUPPORTED services × replicas), readiness does not depend on services, `resources: {}` means healthy, no access log by design, seeders untracked and not in the artifact, runbook drift (`concurrency`, `--no-*`) fixed. And, from the user: **Biome** formats TypeScript/JavaScript/JSON at 100 columns in `make fmt`/`fmt-check`/CI — the SDK and fixtures had grown 200-column one-liners because only Rust had a formatter. A sixth run took the persona nobody had played: an **API consumer** (frontend developer) forbidden to read any server code, working from `/_usai/docs`, the OpenAPI document and a running `examples/invoicing`; 22 frictions, the runtime-level ones fixed: **an authenticated WebSocket refused the credential *after* the 101** (the client saw a bare close frame, `1005 ""`, and could not tell "log in again" from "retry"; the runtime counted it as a success) — the world now runs its auth resolver before the upgrade completes (`socket.accept`), a refusal is the HTTP status, and **browsers can authenticate** with `new WebSocket(url, ["bearer", token])` (the token rides in `Sec-WebSocket-Protocol`, the subprotocol is echoed); the socket's `incoming`/`outgoing` schemas are in the OpenAPI (`x-usai-socket`) and on the docs page; **undeclared 500 codes** (`sql_22003`, `invalid_param`, a SQLSTATE and a parameter position) are `internal` to the client outside diagnostics; `405` carries `Allow`; a non-JSON body on a JSON contract is `415 unsupported_media_type` instead of "null is not of type object"; event streams carry `Cache-Control: no-cache`; the OpenAPI names every runtime code (`invalid_json`, `method_not_allowed`, `payload_too_large`, `unsupported_media_type`, `upgrade_required`) and per-operation 413/415. Also **`USAI_NO_CRON=1` and the other documented environment forms of boolean flags were refused** (clap parsed only `true`/`false`) — fixed with a test. In the example: a schema-valid invoice (100 × 10 000 × 1e8 cents) had overflowed the `::int` cast on read and broken `GET /invoices` for the tenant (bigint end to end now), `dueDate` is a real date at the boundary (`2026-02-30` is a 400), `POST /invoices/{id}/void` exists (the enum promised `void` with no way to reach it; `DELETE` is "Delete a draft"), `/logout` and the webhook are typed, `/me` returns `tenantName`, webhook URLs are http(s) only. [GUIDE §9](/docs/guide/#9-streams-and-websockets) documents browser credentials and the CORS-at-the-proxy rule, §14 what the app listener exposes. A seventh run (2026-09-20, **an Express developer porting a real `notes-service`** — cookie sessions, CORS, helmet, morgan, pagination, multipart upload, a fire-and-forget email job, static files, graceful shutdown, tests, `run --artifact`, two deliberate mistakes; ten minutes from the Express app to the working port, no runtime bug in the port itself) reported 3 bugs, 10 frictions and 8 gaps: fixed — **`auth.custom` was described in OpenAPI as an `Authorization` header** whatever the resolver read (now `credential: { in: "cookie" | "header" | "query", name }` declares it and the document says exactly that, or says "custom" and invents nothing; the reference's panel sends a cookie with `credentials: include`), the body bound is a documented knob (`USAI_MAX_BODY_BYTES`, named in the 413), [GUIDE §4](/docs/guide/#4-http)/§7/§14 gained cookies, uploads, binary columns, the default deadline and "what the proxy owns" (CORS, security headers, static files, access log — one Caddy block), and §14 says the drain grace is `run`'s (`dev` closes at once). Recorded as breadth, not built: cookie parse/sign helpers, a multipart parser, an app-wide response-header hook, binary SQL parameters, bytes in the test harness. An eighth run (**an engineering lead deciding on a pilot from the documentation alone**, two hours, nothing run) approved a pilot with conditions and returned 18 unanswered questions and 15 contradictions between documents — all 15 fixed (release dates, three different "hello at c=16" numbers → conditions stated and the GUIDE no longer quotes a bare number, the soak's real request rate (≈405 req/s, not ≈1 000), the runbook's numbers aligned with the evidence file's final run, the threat model's memory bound pointing at the reference engine and contradicting itself on signing, `usai_world_budget` vs `usai_worlds_live`, three different project statuses, a broken runbook link, C1–C13 → C1–C18, the 1 h soak wrongly listed under the invoicing deployment); of the questions, the two that were runbooks are written ([`docs/runbooks/metrics.md`](/docs/runbooks/metrics/) — every metric with labels and the alerts to set; [`docs/runbooks/systemd.md`](/docs/runbooks/systemd/) — deploy, rolling restart, rollback and runtime upgrade on a plain VM), the default deadline and queue-depth SQL are documented, and the rest (request-id propagation, PostgreSQL feature coverage in the driver, a sizing guide for `--max-worlds` × `pool.max` × `concurrency`, queue throughput numbers, a fuzzing report) are listed under Known gaps. A ninth run (**a data/batch engineer moving a "ledger import" service from FastAPI + Celery**: CSV multipart upload, bigint balances, keyset pagination, a CSV export stream, an exclusive nightly cron verified on two instances, a command, a seeder, a 20 s dispatched task interrupted by SIGTERM, two harness tests with a bytes body, `run --artifact`, three deliberate mistakes — 18 minutes, all ten items done) reported 2 bugs, 11 frictions, 6 gaps; fixed the same day: **`TextDecoder.decode` of 2 MB exceeded the 5 s synchronous CPU slice** (the bridge's codecs were quadratic and shadowed QuickJS-ng's native `atob`/`btoa`; UTF-8 and base64 are now native in the core, `__usai_native` — a 3 MB decode went from a fault to 0.2 s), **a stream handler that failed after `start()` was silent** (now an ERROR line and `usai_http_streams_failed_total`), `http.stream({ contentType })` for CSV/NDJSON streams (response and OpenAPI follow it), integer parameters accept the numeric strings that bigint results return, the cron ledger names the instance (`host:pid`), a drained task is logged as `cancelled: <reason>` rather than `faulted / no outcome`, `/_usai/*` on a listener without the surfaces says where they live, and **a drain now asks background worlds to stop before the bound cancels them** (tasks, cron ticks and queue messages see `ctx.signal` — the report's 20 s task had been killed mid-write with no chance to record it). [GUIDE §12](/docs/guide/#12-what-a-world-can-use) documents the CPU slice (5 s, reset by any host operation, not configurable) and the interpreter's per-iteration cost. Left: `ctx.log(msg, obj)` is not structured under `--log-format json`; the scaffold's placeholders bite anyone who copies the template by hand instead of `create-usai`. A tenth run (2026-09-23, **a platform/SRE deploying 0.0.8 for the first time**, published binary + systemd + a proxy, two replicas, signed artifact, a token-gated status listener, a rolling upgrade, a rollback, a database outage and a `detached_work` incident) would run it in production and measured the drain narration, the upgrade and the rollback exactly as documented (145/145, 168/168, 167/167, 133/133, 124/124 × 200, first 200 back 18 ms after the database returned). Both serious findings were at the **proxy boundary, in the guidance rather than the runtime**, and both are fixed: the published multi-replica health check could not express one status port per replica and so health-checked every replica on replica 1's port (three 503s while a healthy replica sat idle) — the N-replica proxy block is now **in the runbook**, with one loopback address per replica; and health-checking `/_usai/ready` couples every route to every bound resource, so a shared PostgreSQL blip is a *total* outage rather than the partial one `postgres-down.md` promised — documented in both runbooks, with `USAI_READY_REQUIRES_RESOURCES=0` for deployments that would rather keep resource-free routes serving. Runtime fixes from the same round: the public listener answered **401** for `/_usai/status` and `/_usai/metrics` when a status token was set (announcing an operator surface to anyone; now 404 like every other unserved path, with a test), a readiness probe that *hung* never reached the resource's health fold so `usai_resource{metric="ready"}` stayed at 1 until the next real query (the bound is now inside the resource), and the startup banner advertised surfaces `USAI_SURFACES_OFF` had removed. Docs: the runbooks quoted log lines that do not exist (`lifecycle violation`) — a violation's message *is* the explanation, the grepable fact is `code=`; `LimitAS`/`ulimit -v` must not be set (≈200 GB of reserved address space for 48 worlds); the scaffold now ships `migrations/0001_notes.sql` and a commented `postgres()` block so there is a production migration path to run before writing code; [`SUPPORTED.md`](/docs/supported/) explains why `create-usai` has its own version line. Not covered: the container images — the round's box had no working Docker, so it ran the systemd path instead.
- **Tutorial application** `examples/todos` (modules, colocated migrations and seeders, typed env, HTTP CRUD with boundary contracts, dispatched task, cron, command; `usai/test` with `migrate: { seed: true }`); [`docs/GUIDE.md` §16](/docs/guide/#16-a-realistic-application-examplestodos) walks it. Runs in CI against the service database.
- **PostgreSQL TLS** (rustls, `sslmode` from the URL, roots = Mozilla + `tls.caFile`/`PGSSLROOTCERT`, always verified; refused at activation otherwise). Tested against an embedded TLS server with a private CA.
- **`usai dev` reload acceptance** (`crates/usai-cli/tests/dev_reload.rs`): edit → new revision, no failed request during the swap; a broken edit keeps the previous revision serving. Control surface: rollback = reinstall + activate (tested).
- **P1/P2 substrate economics.** Per-phase ledger (`USAI_PROFILE=1`), workload matrix (`tests/profile_matrix.rs`, `scripts/p1-attribution.sh`), research-VM attribution; slot-reset root fix (vendored Wasmtime patch), callback-free validator preparation in the image (`runtime/prepare.ts`), zero-delay timer yield, lock-free watchdog. Hello on the VM: 1.52 ms p50, 9.8k req/s at c=16, 0 faults.
- **P0 substrate (ADR-0016).** `engine/wasm.rs`: Wasmtime 48 + core built from pinned sources (`guest/build.sh`, `-O3`; `-Oz` reproduces the research core byte for byte) + Wizer image per definition + pooling/COW/pagemap_scan; guest ABI unchanged (bridge routes through the core's `__usai_test_op`); one artifact (IIFE bundle) serves both engines; compilation cache; epoch-based CPU slice. `wasm` is the default engine, `quickjs` selectable. Every acceptance suite passes on both (121 Rust test functions and 30 TS cases as of 2026-09-23; the reference engine runs lifecycle and http). Profiling harness: `tests/profile_bundles.rs` (`--ignored`, release).
- Design review closed 14 of 16 open questions as ADR-0001…0014; ADR-0015 records the engine decision.

## In progress

- nothing

## Measured (2026-09-17, release build, this machine, `usai bench` on `examples/hello`, engineering numbers — not canonical evidence)

```text
c=1    p50 8.2 ms   p99 9.9 ms    121 req/s
c=16   p50 30 ms    p99 45 ms     514 req/s    RSS high-water 133 MB
```

Attribution (`tests/profile_world.rs`, `cargo test --release --test profile_world -- --ignored --nocapture`):

```text
engine floor (fresh runtime + context)        0.88 ms/world
tiny module                                   0.72 ms/world
hello bundle (765 KB, zod evaluated per world) 6.52 ms/world
```

~90% of per-world cost is **application module evaluation per world** — exactly the "definition-level work rebuilt per world" the research removed with a pre-initialized image (Wizer) + copy-on-write memory (C13, EXP-011B/012B). The native QuickJS substrate (ADR-0015) has no snapshot mechanism, so this cost is structural to v0's engine choice, not to the lifecycle model. This is the concrete trigger ADR-0015 named for revisiting the substrate.

- **API reference** (`/_usai/docs`, 2026-09-18): a dependency-free page (works offline, light/dark, keyboard, 320 px) that renders the OpenAPI document plus the Usai facts now carried as `x-usai-*` extensions — validated-before-world slots, lifetime and effective deadline, resources leased, tasks handed off, declared errors, and the non-HTTP workloads, resources and environment of the application. Schema tables instead of JSON dumps, per-status responses, copyable curl, Try-it with `x-usai-server-ms`. Served in dev and with `--status`.

- **P3.5 Distribution & container UX** (2026-09-18): `docker/runtime.Dockerfile` (debian-slim + stripped `usai`, user 10001, `USAI_COMPILE_CACHE=0`, read-only ok, ~130 MB) and `docker/dev.Dockerfile` (runtime + Node 24 + pnpm + git); release publishes both for linux/amd64+arm64 to Docker Hub `sakaladev/usai` and GHCR from the same tag as binaries and npm; scaffold ships `compose.yaml` (dev path, installs on first start) and a two-stage `Dockerfile`; SIGTERM drains; `--artifact` needs no project/config; artifact records `builtWith {sdk, runtime}` and an unsupported format is refused before serving with an actionable message; `scripts/container-smoke.sh` runs in CI and after every release (scaffold → app image → read-only non-root → 200 → SIGTERM → compose path). `@sakaladev/usai` ships a `usai` bin: `pnpm dev` runs the runtime at the SDK's version (a same-version `usai` on PATH, else the release asset fetched once and SHA-256 verified) — the non-Docker path needs no manual install either. Roadmap to production-ready in [`docs/ROADMAP.md`](/docs/roadmap/); governance in [`GOVERNANCE.md`](/docs/governance/).

- **P4 primitives (ADR-0017)**: `httpClient(...)` resource (owned, bounded, origin-pinned outbound HTTP; no global `fetch`), `crypto` WebCrypto subset with per-world host entropy (`randomUUID`, `getRandomValues`, SHA-2 digests, HMAC), `password.hash/verify` (Argon2id host op), `db.transaction(fn)` as one owned operation (holder task, rollback for a world that ends with it open, teaching diagnostic). Acceptance tests in `tests/http.rs` and `tests/postgres.rs`. Precompiled-image fingerprint now covers the bridge.

- **Second fresh-eyes run (v0.0.4, a subagent as an outside developer building a notes API)**: 2 blockers + 12 frictions, all closed: config evaluated in a world is now a teaching error (`ConfigNotDeclarative`); a killed wrapper no longer orphans the server (`USAI_PARENT_PID` watch) and a bind failure names the real cause; `.env` re-read on every rebuild; `usai run` says it does not read `.env`; PostgreSQL text timestamps round-trip; connection errors name host/user/reason; **source maps** (`app.js.map`, `usai:app:L:C` → `src/x.ts:L:C` in stacks); `usai build` type-checks (fatal, `--no-typecheck`) and `usai dev` type-checks in the background; reload prints the added/removed workloads; `curl /_usai/docs` gets the OpenAPI document; the test harness's runtime is quiet; task-failure logs are plain text; `cache.local` scope documented; `usai dev` installs the image it just compiled instead of compiling again.

- **P4 application** `examples/invoicing` ([GUIDE §17](/docs/guide/#17-the-p4-application-examplesinvoicing)): built as a user of the SDK; its test runs in CI against the service database. `publishes(...)` joins `dispatches(...)` for the graph/docs; PostgreSQL enums round-trip as labels.

- **P7 round on 0.0.5 (three subagent personas, public docs + npm/Docker Hub only)**. Fixed from their reports: a write left un-awaited answered 200 while the runtime cancelled it → **500 `detached_work`** when side effects were cut (timers still commit with the diagnostic); application `console.*`/`ctx.log.*` were DEBUG-only → INFO under target `app`, and compiler internals no longer flood `RUST_LOG=debug`; `--log-format json` was parsed and ignored → works, logs on stderr with timestamp+level on every line (the shutdown narration too); `/_usai/ready` (bounded resource probes, 503 names the resource) and `/_usai/live`; `--status-addr` / `USAI_STATUS_ADDR` for a private status listener; per-workload response counters and process metrics (`usai_build_info`, RSS, fds, start time); the scaffold's Dockerfile could not build after `pnpm install` (pnpm rewrites `pnpm-workspace.yaml` mode 0600) → `COPY --chown`; unsupported parameter types (`interval`, `inet`, …) take their text form; validation messages no longer leak schema regexes; `/_usai/docs` answers HTML unless JSON is asked for; stale `signature.json` removed on rebuild, `*.key` git-ignored, signature verification logs an audit line; raw endpoints declare `errors`/`responses`; runtime pool errors name host and user; resources no revision binds are released (status stops listing them); the npm wrapper exits when its own parent dies; release images are built from the published binaries (identical bits to the tarball). Docs: row types, SQLSTATE errors, auth resolvers' resources, config example, production compose (`docs/deploy/compose.production.yaml`), runbook wording aligned with the logs.

## Dogfood (2026-09-18, fresh-eyes external-user run against v0.0.1)

A reviewer with no prior knowledge installed from npm + the release, built a
bookmarks backend (PostgreSQL, migrations, seeder, dispatched task, cron,
command, typed env, harness test) from the public docs only, and filed 21
frictions (3 blockers). Fixed for 0.0.2: worlds have `URL`/`URLSearchParams`/
`structuredClone` (+ `@sakaladev/usai/globals` types; `fetch`/`crypto`
documented as absent with reasons); `usai dev` no longer rebuilds forever
(inotify Access events) and skips an unchanged definition; `usai test` works
from the published package; deadlines bound synchronous work; un-awaited
`dispatch` is diagnosed correctly; undeclared resources and holes in
declaration lists are named errors; response-contract failures carry their
issues; issue paths are JSON pointers everywhere; `.env` for local commands;
one-shot commands are quiet and the config is cached; `run --status` banner;
inspect wording; scaffold ships `.gitignore`, `skipLibCheck`, `allowBuilds`.
Still open from the report: outbound HTTP (`fetch`) and `crypto` as host
capabilities (design), latency histogram in metrics, an API reference page — all three shipped since: ADR-0017 for `httpClient`/`crypto`, `usai_http_request_seconds` in the metrics, and `/_usai/docs`.

## Next

1. **Threat model verified, not just written** (2026-09-23, [`docs/measurements/2026-09-23-threat-verification.md`](/docs/measurements/2026-09-23-threat-verification/)): `scripts/qualification/threat/run.sh` checks every claim in [`docs/THREAT-MODEL.md`](/docs/threat-model/) that is visible from outside the process — 39 probes as of the evening's two additions, all passing, now part of the release gate. The two bugs it found were in the instrument (a signature probe that read a file `keygen` never writes; a “no world was created” check against a gauge that was still zero). Not covered there, by design: the socket idle timeout (connection campaign), admission refusal (hardening suite), the routing gate (lifecycle suite), connection quarantine (postgres suite).
1. ~~Propose the Wasmtime pagemap-reset patch upstream~~ — **merged into `bytecodealliance/wasmtime` main on 2026-09-23** (#14357, `a2e2d86`): review answered the same day, the maintainer called the fix and the speed-up reasonable and classed the paged-out-page hole as a bugfix. What is left is mechanical: the first wasmtime release after the merge (v49.0.0 predates it) replaces the vendor — re-vendor that version, drop `vendor/wasmtime` and the patch, keep the freshness tests ([`docs/upstream/wasmtime-pagemap-reset.md`](/docs/upstream/wasmtime-pagemap-reset/)); the vendor stays until a release carries it. ~~The eval-based invoke/outcome/pending floor~~ — gone (ADR-0018; P8 c=1 phase done, [`docs/measurements/2026-09-19-p8-parity.md`](/docs/measurements/2026-09-19-p8-parity/)).
2. **Running now on VM 47** (2026-09-23, the production-readiness campaign): part 1 done — comparator floors (php 4/4 valid, and the limits are now *verified* against the daemon rather than assumed; node and our own template cells failed for reasons of our own, both fixed and re-running), the queue cells (the c=4 ≈ c=8 plateau is closed: 432 → 741 → 1 107 msg/s) and **saturation to c=512** ([`docs/measurements/2026-09-23-saturation-and-queue.md`](/docs/measurements/2026-09-23-saturation-and-queue/): flat throughput, linear latency, no 5xx below 512, refusal rather than collapse at 512). Part 2 is queued behind it: the floors re-run, **three replicas** on the published 0.0.8 images ([`SUPPORTED.md`](/docs/supported/) claims N and every campaign had run two), then a **24 h soak with a bounded dataset** — the control run the 72 h soak never had, which is what decides whether its 794 → 154 req/s decay was the data or the runtime.
3. Waiting on wall clock: ~~24 h soak → `conn-churn`~~ → ~~**72 h soak**~~ — all passed and appended to `2026-09-18-p5-p6-qualification.md` (the 72 h run on 2026-09-23: 73.6 M requests, 0 × 5xx, 0 quarantined, flat memory). The sweep ran on the idle VM and is written up ([`docs/measurements/2026-09-23-sweep.md`](/docs/measurements/2026-09-23-sweep/)); the P9 decision is ADR-0019. **v0.0.6 and v0.0.7 are released** (2026-09-23).
4. Waiting on humans: P7 external developers (the internal fresh-eyes rounds are the rehearsal, not the evidence). **The kit exists**: [`docs/P7-EXTERNAL-VALIDATION.md`](/docs/p7-external-validation/) (the 90-minute task sheet and what to record) and the `P7 report` issue template — hand it to three developers on the current release.
5. Acceptance audit done ([`docs/ACCEPTANCE-AUDIT.md`](/docs/acceptance-audit/)); the one remaining ◐, crash/restart recovery, is the orchestrator's by design (D15; queue messages survive, in-memory dispatches are counted lost). ~~Tutorial application~~ — [GUIDE §16](/docs/guide/#16-a-realistic-application-examplestodos) walks `examples/todos`, §17 `examples/invoicing`. ~~First tag~~ — v0.0.1…v0.0.5 shipped. ~~Per-world CPU accounting~~ — done (`WorkResult.cpu`, `usai_guest_cpu_seconds_total`; fairness/budgets stay a D13 follow-up, [`docs/THREAT-MODEL.md`](/docs/threat-model/)).
6. Released: `v0.0.1`–`v0.0.7` (the last two on 2026-09-23) — `release.yml` publishes binaries, npm `@sakaladev/usai` + `@sakaladev/create-usai` (Trusted Publishing/OIDC; `workflow_dispatch` for scaffolder-only fixes) and Docker images (Docker Hub `sakaladev/usai`, the canonical address, and GHCR `ghcr.io/gmedia/usai` as a public mirror). v0.0.4 verified from the registries: `docker pull` on amd64 and arm64, the container smoke against Docker Hub, and the pure-npm path (`pnpm dlx @sakaladev/create-usai` → `pnpm install` → `pnpm dev` fetching the release binary → `pnpm test`). The unscoped `usai` name is refused by npm as too similar to existing packages. ~~Open: artifact signing (ADR-0005 follow-up)~~ — closed 2026-09-23: application artifacts sign and verify (the threat campaign proves a correctly signed artifact runs, a tampered one is refused and another key's is refused), and the release itself now carries Sigstore build provenance on every tarball and image (`gh attestation verify … --repo gmedia/usai`). **0.0.6 is withdrawn in effect**: its Linux binaries and images require `GLIBC_2.39` (built on the CI runner, not on the glibc floor) and do not start on Debian 12 — which is what its own container-smoke job caught, after publishing. 0.0.7 builds them inside `rust:1.98-bookworm` and the release refuses a binary above the floor. Every tarball up to 0.0.6 had the same defect; only the images hid it.

## Known gaps / debt

- `usai dev` compiles each rebuilt image once (the build's compiled form is installed directly). Measured 2026-09-23 on an idle dev box: **a source change is serving again in ≈3 s**, of which ≈1.6 s is the image compile; a change that does not alter the manifest is recognised and keeps the running revision. (Round 13 reported 9–12 s — measured on a box that still had the session's own load on it, like its first-request numbers.) The compile still takes every core for those seconds on a small host, and the obvious way to make it faster — skipping the pre-initialisation that makes worlds cheap — would make `dev` and `run` behave differently, which is the one thing a development loop must not do.

- The Wasm substrate is the research representation (ADR-0016); its per-request cost is attributed and, after P8, halved on the VM (`2026-09-19-p8-parity.md`): what remains is the interpreter running the developer surface, the boundary JSON and the fresh world itself. Do not cite the research programme's numbers for this codebase; cite the suite's. The native QuickJS engine stays as reference; do not use it for economics.
- Boundary contracts were validated twice when a JSON Schema exists (host before the world, provider inside it to obtain parsed values). Since P8/C2b the SDK proves per slot whether the provider's output is its input (`hostFinal`: structural types, library checks, defaults; no transforms/catch/prefault/readonly; recorded as `contracts.boundaryFinal`); the host tells the world which slots it validated (`request.validated`) and the world replaces the second parse with an exact finalizer (runs the same library checks, strips undeclared keys, falls back to the full parse whenever a check disagrees). Slots that change the value keep the double pass by design (`GOAL.md` §12).
- Auth resolvers run inside the world (after structural validation); `inspect` says so.
- ~~Module-level `migrations:`/`seeders:` globs are root-relative~~ — **fixed 2026-09-23**: the build stamps each `defineModule` call with the directory it was written in, so a module's globs may be written relative to the module itself (`./migrations/*.sql`). A glob is still tried as written first, so every application built before this is unaffected, and the “matches no file” warning now fires only when neither form matches.
- HTTP/1.1 only; no TLS termination (expected behind a proxy in v0).
- From the seventh and eighth fresh-eyes rounds (2026-09-20), built the same day (user decision: everything that does not wait for the VM): `auth.cookie` + `cookies` helpers, repeated `Set-Cookie`, `Uint8Array` ↔ `bytea` + `bytes`, bytes in the test harness, request ids end to end, `cron({ exclusive: true })` claimed in PostgreSQL, `multipart.parse`, `defineApp({ headers })`. Also written: [`docs/runbooks/sizing.md`](/docs/runbooks/sizing/) and the PostgreSQL coverage table in [GUIDE §7](/docs/guide/#7-postgresql) (probed: cursors, `SET LOCAL`, savepoints, advisory locks, temp tables work; COPY and LISTEN/NOTIFY do not; `bigint` beyond ±2^53 now travels as a string — it used to round). Fuzzing report written: [`docs/measurements/2026-09-20-fuzzing.md`](/docs/measurements/2026-09-20-fuzzing/) (3 × 20 min, 78 M executions, 0 crashes). Queue throughput measured (`scripts/qualification/queue`, four runs on 2026-09-20 in the P5/P6 report: 93 → 712 msg/s at c=16 on one instance after three fixes the campaign exposed — schema DDL per publish, a sorting claim, synchronous marks).
- Ninth round (2026-09-20): a **runbook drill** — an SRE persona took the pager with the runbooks only and induced each incident (PostgreSQL kill −9 and clean restart, a webhook endpoint answering 500 until dead-letter, a login burst, capacity refusals, a drain under load, a rollback). Verdict: the runtime behaved better than its runbooks. Fixed the same day: JSON log lines carried two `message` keys on a 5xx (the error text is now `error`); `usai_http_request_seconds` counted refusals decided before a world (now admitted-only, as `metrics.md` had promised); `/_usai/status` → `resources[].ready` stayed true through an outage (now follows the last contact: false on a connection-level failure with `lastError`, true after the next success); a database outage wrote an `ERROR` + stack per request (now one `WARN dependency unavailable … suppressed=<n>` per code per second); glibc kept 150–200 MiB after an Argon2 burst and after a revision removal (`malloc_trim` after both); activating the active revision again silently started a second scheduler (refused, `409 invalid_state`); `invalid_state` messages said `is Active, not active` (now `is active, already active` / `only an installed or retired revision can be removed`); a timed-out drain never logged `revision retired` (now `revision retired cancelled_in_flight=<n>`); `usai inspect` shows `pool.max` with its default. Runbooks corrected where they were wrong (postgres-down: idle connections vanish, only in-flight ones are quarantined, `sql_57p01` beside `connection_closed`; runtime-restart: what the second and third signal do, 499 on the drain-timeout cancel; metrics: `usai_service`, `usai_http_streams_failed_total`, lowercase state labels, the workload label format; sizing/memory-pressure: per-workload memory; systemd: a real `%i` template unit; the compose Caddy block: `health_interval 1s`). Two references the drill wanted and could not find, written: [`docs/ENVIRONMENT.md`](/docs/environment/) (every `USAI_*`) and [`docs/CONTROL-API.md`](/docs/control-api/).
- Tenth round (2026-09-20): a **mobile-backend developer** (ex NestJS + Prisma) built an auth-heavy API — signup/login, HMAC access tokens, refresh rotation with reuse detection, lockout after 5 failures, forgot/reset by a dispatched task, device registration, an API-key admin route — from the public docs in 12 minutes of hands-on time, 10/10 tests green on the first run, and would ship a v1 on it. No blockers. Fixed the same day: **the request id now follows the hand-off** (a task the request invokes or dispatches runs with the same id — `ctx.requestId` in the task, `request_id` on its log lines); **`tokens.sign/verify`** in the SDK (the helper every mobile backend was going to hand-roll); the test harness keeps the runtime's log — **`app.logs(filter)` / `app.waitForLog(filter)`** — so a test observes a dispatched task by request id instead of shelling out to `psql`; all missing environment variables are reported in one message (not one per restart); `KeyUsage`/`CryptoKey`/`HmacImportParams` in the world's globals; the `AuthRequest` doc no longer says "no world yet"; `x-usai-validated` and `usai inspect` say when a transforming schema is parsed again in the world (`both`); a GET without a body no longer promises `invalid_json`; the 401 description names `unauthorized`. GUIDE: access tokens, the lockout recipe and per-IP limits (§4), "decide inside the transaction, throw outside" (§7), hand-off ids (§14), log access in tests and `--conditions=usai` (§15). Also built: **an auth scheme declares the resources its resolver leases** (`auth.bearer({ resources: [db], resolve })`) — typed on the resolver's `ctx.resources`, inherited by every workload that uses the scheme, shown on the operation. A queue message now carries its publisher's request id as well (`usai_queue.request_id`, added idempotently to existing tables). `z.strictObject` at the boundary verified (400 naming the key) and documented in §4.
- **First external developer (2026-09-23).** Not a persona: a working Express developer, asked by the maintainer to try Usai, reported back informally (secondhand, no timings, no written report — recorded here because it is the first data point from someone outside the project, and it is worth more than the rehearsals below). He got there — "adaptasinya lumayan, akhirnya paham cara kerjanya" — with no blocker, and the friction was all in the shape of the code, not the runtime:
  1. `defineApp` and modules confused him at the start; he could not see where his code was supposed to go.
  2. He wanted his Express layout — routes in their own files, handlers/controllers separate — and struggled to map it. The GUIDE's §19 maps *concepts* (middleware → auth, `multer` → `multipart`); it does not map the **file layout**, and §3 shows one module without showing where a route's handler may live.
  3. He tried to discover modules the way Express does: a glob, `node:fs`, dynamic `import()`. Reproduced here on 0.0.7 — the build says `Could not resolve "node:fs"` and `Top-level await is currently not supported with the "iife" output format`. The first is true but does not teach (why there is no filesystem, that imports must be static, what to do instead); the second is esbuild's internals reaching the user.
  4. He tried **knex** as the ORM. The docs say "no ORM, plain SQL" (§19) but never say what happens if you bring one, nor whether a query *builder* used only to compile SQL is a usable pattern.
  5. He noticed the first response took some milliseconds and everything after it was fast — correct behaviour (a fresh revision instantiates its image and opens its pool on the first request; P8E measured a cold first response at 11–16 ms against Node's 45–62), but nothing in the docs prepares a reader for it.
  6. **Autocomplete misled him**: the editor kept offering Express shapes and he assumed they applied. Recorded as the first evidence that the type surface, not just the prose, has to teach the difference.
  This is one person, not the three to ten P7 asks for ([`docs/ROADMAP.md`](/docs/roadmap/)), and it does not close the gate. Two subagent rounds were started the same day on exactly these frictions (layout, auto-discovery, query builders, first-request latency, the editor experience) to find what he did not have time to.
- Twelfth round (2026-09-23): a **platform engineer planning the 0.0.5 → 0.0.6 upgrade** from the release notes and the runbooks alone, with alerts, a Loki pipeline and a queue in production. The release note held up (both artifact directions, the one changed default, the honest p99 regression) but hid the one step that could take a queue down: **the two new `usai_queue` indexes build under a write-blocking lock on first use of the new version**, on a table the runtime never prunes. Fixed: the Compatibility section now gives the three statements to run beforehand with `CREATE INDEX CONCURRENTLY`, says a 0.0.5 runtime ignores them, and opens with the five ordered upgrade steps; a slow schema statement logs what it was building and for how long; `systemd.md` no longer claims the queue table is "stable across versions". Also from that round: `usai_http_request_seconds_count` is not a request rate any more (a throughput panel built on it now falls during a bad-traffic flood) and the 101-is-not-a-5xx change moves a 5xx baseline — both now in Compatibility; the WebSocket 401-before-101 is a client-visible change and is now listed; `fields` is a JSON string, not an object ([GUIDE §14](/docs/guide/#14-operate) and Compatibility say so); `metrics.md` gave a workload label shape the runtime never emits (`http:listInvoices` — it is `http:GET /invoices`); `--log-format json` left the human banner on stdout, which was a parse error per start for a shipper reading the journal — `usai run` now keeps stdout empty and logs one `serving` line instead.
- Eleventh round (2026-09-20): a **front-end / BFF developer** (React + TanStack Query, ex-tRPC) built the backend for a browser SPA — cookie login, keyset pagination, ETag/304, raw image upload/download, SSE with resume, a cookie-authenticated WebSocket, a streamed CSV — in 17 minutes, 8/8 tests green, and evaluated the OpenAPI document as `openapi-typescript` input and `/_usai/docs` as a consumer. Verdict: would point a production SPA at it behind a proxy that owns CORS. Fixed the same day: a client leaving an event stream was logged as `ERROR` and counted in `usai_http_streams_failed_total` (now the normal end of a stream); **declared errors are typed in the document** (`error.code` an enum per status; every declared status carries its reason phrase — a `401` in `response:` was "Success"); a raw `GET` emitted a `requestBody` and no path parameters; a missing required property was reported at pointer `""` instead of `/name`; the socket description assumed a bearer token for cookie-authenticated sockets (the reference page and the document now say what the browser does per scheme); the reference's Responses panel explains CSV/raw/bodiless statuses; `x-accel-buffering` was duplicated when a handler set it; `cookies.sign` refused values containing `.` (lifted — verify splits on the last dot); `stream.event(name, data, { id, retry })` for `EventSource` resume; `http.notModified()` and bodiless responses typed for any contract; stream routes honour the boundary-final proof like HTTP routes (`inspect` said "parsed again" for an identical schema); the test harness gained **`app.stream()`** (SSE event by event, or `text()`) and **`app.socket()`** (a WebSocket with cookies or `["bearer", token]`, `TestSocketRefused` on 401). Docs: CORS in development (Vite proxy, or `http.options` + `defineApp({ headers })`), Secure cookies on localhost/Safari and the SameSite/CSRF posture ([GUIDE §4](/docs/guide/#4-http), THREAT-MODEL), declare errors not error schemas, SSE ids. Then built: `responseHeaders: { 201: { location: "…" }, "*": { etag: "…" } }` documents an endpoint's response headers per status in the OpenAPI document and the reference. Also built: `operationId` on every endpoint, and a socket's message contracts as `components.schemas.<OperationId>Incoming/Outgoing` in both profiles. Catch-all routes (`/files/*path`, `http.options("/*any")`) existed in the router but were undocumented and lost to a literal path's 405 — a catch-all now serves the methods a literal lacks, and an unknown URL stays 404; documented in §4. SSE event schemas built: `http.stream({ events: { tick: schema } })` validates each `stream.event` in the world and documents the events as `components.schemas.<OperationId>Event<Name>`. Stale clients: documented (§4) rather than built — `defineApp({ headers: { "x-app-version" } })` and the public document's `info.version` already answer it; a runtime revision id is an operator fact, not a client contract.

